# 既存教材統合 調査・設計書

> 作成: 2026-08-25 / 対象: **Phase 7 完成版**（`main` = `1e72e47` / Vercel Production 反映済み）
> ステータス: **調査・設計のみ。コード変更・migration・Supabase 変更は未実施**
>
> この資料は単体で読めるように書いてある。
> 併せて読むなら `FURICARI_HANDOFF.md`（現状）と `PHASE7_ADMIN_SPEC.md`（管理画面の設計）。

---

## 1. 結論

### 統合を推奨する。ただし「Lesson を 5 種類に分ける」設計は採らない。

現在の `lessons` は、**すでに動画を必須としていない**。
`stream_video_id` は nullable で、実際に 90 本中 89 本が null のまま公開されている。
Phase 7 の公開判定にも「動画を持たないレッスンは公開できる」という分岐が既に入っている。

```ts
// src/lib/stream/video-state.ts
export function canPublishLesson(lesson) {
  if (!lesson.streamVideoId) return { ok: true };   // テキスト教材を締め出さない
  ...
}
```

つまり土台は「動画レッスン専用」ではない。
足りないのは **本文（テキスト・画像・ファイル・リンク）を持つ場所** だけである。

### 推奨する形

**`lessons` に `lesson_type` と `body`（jsonb のブロック配列）を足す。それだけ。**

- 別テーブルを作らない
- レッスン種別を 5 つに増やさない（**2 つ**にする）
- 既存の 90 本は一切変更しない（`lesson_type` の既定値が `video`）

ブロック配列は Phase 6 のお知らせ（`announcements.body`）で既に実績がある方式で、
描画部品もエディタも考え方をそのまま流用できる。

| 判断 | 理由 |
| --- | --- |
| `lesson_type` は `video` / `article` の **2 値** | 「課題」「ダウンロード」「外部リンク」は**種別ではなくブロック**。1 レッスンに混在するのが自然（課題を出しつつ素材も配る、など） |
| 本文は `jsonb` のブロック配列 | 表を増やさずに済み、ブロック種別を足すのが migration 無しでできる |
| 別テーブルを作らない | 現在の教材規模（90 レッスン）で join を増やす理由がない。取得は 1 リクエスト 1 回のまま |

### 実装規模：**中**

DB 変更は 2 列の追加のみ。作業の大半は
**受講生側の本文描画**と**管理画面のブロックエディタ**で、どちらも既存の実装を流用できる。

---

## 2. 現在の LMS 構造

### 階層

```
Course（6）
  └ Chapter（25）
      └ Lesson（90）
```

### `lessons` テーブル（Phase 7 時点）

| 列 | 型 | 備考 |
| --- | --- | --- |
| `id` | uuid | |
| `slug` | text unique | URL に出る（`/watch/premiere-practice-05`） |
| `course_id` / `chapter_id` | uuid | |
| `number` | integer | 表示用の通し番号（コース内で 1 から連番） |
| `title` | text | |
| `description` | text \| null | カード・「この動画について」 |
| `key_points` | jsonb | 「学べること」の箇条書き |
| **`stream_video_id`** | **text \| null** | **null 可。90 本中 89 本が null** |
| `duration_seconds` | integer | 尺。Cloudflare から同期する |
| `tool_id` / `category_id` | uuid \| null | |
| `level` | enum | beginner / intermediate / advanced |
| `is_published` | boolean | |
| `published_at` | timestamptz | 新着表示用 |
| `sort_order` | integer | |
| `stream_status` | enum \| null | pending / ready / error |
| `stream_synced_at` | timestamptz \| null | |
| `stream_error` | text \| null | |

### Lesson はどこまで動画前提か

**結論：DB は動画前提ではない。UI とバッジ表現が動画前提。**

| 層 | 動画依存度 | 実態 |
| --- | --- | --- |
| DB スキーマ | **低** | `stream_video_id` は nullable。動画が無くても整合する |
| 受講権限 | **なし** | コース単位。レッスン種別と無関係 |
| 進捗 (`lesson_progress`) | **中** | `position_seconds`（再生位置）を持つが、**手動完了の経路が既にある** |
| 完了判定 | **中** | 90% 自動完了は再生位置ベース。ただし `status === "completed"` を直接立てる経路も実装済み |
| 受講生 UI | **高** | `/watch/[lessonId]` はプレイヤーが主役。動画が無いとダミープレイヤーが出る |
| 文言 | **高** | 「この動画について」「視聴済みにする」「次の動画」「学習済み動画」など |
| 管理画面 | **中** | レッスン編集は既に動画と独立。動画カードだけが `stream_video_id` を要求する |

**重要**：`duration_seconds` は 10 ファイル以上で使われている
（サムネイルの尺表示、総学習時間の集計、コース進捗、絞り込み）。
テキストレッスンでここを 0 にすると、集計と表示の両方に影響が出る（→ 10 章）。

---

## 3. 既存教材との違い

| | 現在のフリキャリ | 既存教材（Notion 等） |
| --- | --- | --- |
| 主体 | 動画を見る | テキストを読む・手を動かす |
| 1 単位の中身 | 動画 1 本 + 説明 | 見出し・本文・画像・資料・課題・素材・外部リンクの組み合わせ |
| 完了の意味 | 90% 視聴 | 読み終えた / 課題を出した |
| 所要時間 | 尺で決まる | 決まらない |
| 素材配布 | 無い | ある（PDF・テンプレート・プロジェクトファイル） |
| 外部サービス | 無い | ある（Canva・Google Drive 等） |

**噛み合わない点は 2 つだけ。**

1. **本文を置く場所が無い**（`description` は 1 行の説明で、見出しも画像も持てない）
2. **完了の判定が再生位置に依存している**（テキストには再生位置が無い）

逆に言えば、**それ以外は今の構造で足りている**。
コース / チャプター / 受講権限 / 公開制御 / 並び替え / お気に入り / 履歴 / 検索は
種別に関係なくそのまま使える。

---

## 4. 現状のままで対応できること

追加開発なしで、**今日から**できること。

| できること | 根拠 |
| --- | --- |
| 動画を持たないレッスンを作って公開する | `stream_video_id` は null 可。`canPublishLesson` が許可済み |
| コース / チャプターに混在させる | 種別の概念がそもそも無いため |
| 受講権限で保護する | コース単位。Phase 6-E のまま効く |
| 並び替え・公開切替 | Phase 7-H の管理画面でできる |
| 検索に載せる | タイトル・説明・学べること・チャプター名・コース名が対象 |
| お気に入り / 履歴 / 進捗 | レッスン単位。動画かどうかを見ていない |
| 「学べること」を箇条書きで出す | `key_points` を管理画面から編集できる |

### つまり「ごく短いテキスト教材」なら今すぐ作れる

タイトル + 説明 + 学べること だけで済む教材（例：「はじめに読んでください」）は、
**追加開発ゼロ**で管理画面から作成・公開できる。

ただし現状では **動画が無いレッスンにダミープレイヤーが表示される**ため、
受講生には「再生できない動画」に見える。ここが最初の壁になる（→ 7 章）。

---

## 5. 追加で必要な機能

| # | 機能 | 必須度 | 理由 |
| --- | --- | --- | --- |
| 1 | 本文（ブロック配列）を持つ列 | **必須** | 見出し・段落・画像を置く場所が無い |
| 2 | レッスン種別（`video` / `article`） | **必須** | プレイヤーを出すか本文を出すかの分岐 |
| 3 | 本文の描画（受講生側） | **必須** | |
| 4 | 本文のエディタ（管理画面） | **必須** | 運営が SQL を書かずに済むこと |
| 5 | ファイルの配布 | **必須**（既存教材に素材がある場合） | Supabase Storage を使う |
| 6 | 完了の手動記録 | **既にある** | 「視聴済みにする」を流用。文言だけ変える |
| 7 | 所要時間の手入力 | **推奨** | テキスト教材に尺が無い。集計を壊さないため（→ 10 章） |
| 8 | 課題の提出機能 | **不要（初期）** | 「課題を出す」ではなく「課題を提示する」で足りる。提出・添削は別問題 |

### ファイル配布について

**Cloudflare Stream は動画専用**なので使えない。
**Supabase Storage** を使うのが自然（同じプロジェクト内・RLS で保護できる・追加契約が不要）。

- 非公開バケットに置き、署名付き URL を発行する
- 受講権限のチェックは Phase 6-E と同じ考え方（**サーバー側で判定してから URL を発行**）

---

## 6. 推奨データ構造

### 変更は 2 列だけ

```sql
-- 案（未適用）
create type public.lesson_type as enum ('video', 'article');

alter table public.lessons
  add column if not exists lesson_type public.lesson_type not null default 'video',
  add column if not exists body jsonb not null default '[]'::jsonb;
```

**`default 'video'` にすることで、既存 90 本は 1 行も変更されない。**

### 本文ブロックの型

`announcements.body` と同じ考え方。TypeScript 側の型は次を想定する。

```ts
export type LessonBlock =
  | { type: "heading";   text: string }
  | { type: "paragraph"; text: string }
  | { type: "list";      title?: string; items: string[] }
  | { type: "image";     path: string; alt: string; caption?: string }
  | { type: "file";      path: string; label: string; note?: string }
  | { type: "link";      href: string; label: string; note?: string }
  | { type: "callout";   tone: "info" | "task"; title: string; body: string };
```

- `image` / `file` の `path` は **Supabase Storage 内のパス**。URL を直に持たない
  （バケットやドメインが変わったときに全レコードを書き換える羽目になるため）
- `callout` の `tone: "task"` が「課題」を表す。**課題を種別にしない**

### なぜ「別テーブル」にしないのか

`lesson_blocks` テーブルを作る案も検討したが、採らない。

| | jsonb 1 列 | 別テーブル |
| --- | --- | --- |
| 取得 | 既存の 1 回のクエリに含まれる | join か 2 回目のクエリが要る |
| 並び替え | 配列の順序そのもの | `sort_order` の管理が要る |
| ブロック種別の追加 | migration 不要 | 列追加や型変更が要る |
| 部分更新 | レッスン単位で書き換え | ブロック単位で更新できる |
| 検索（本文全文） | やや不利 | 有利 |

現在の規模（90 レッスン、1 リクエストで全件メモリに載せている）では
**取得の単純さが勝つ**。本文の全文検索が必要になったら、
そのときに `body` から生成列を作るか、別テーブルへ切り出せばよい。

### なぜ「5 種類の lesson_type」にしないのか

「課題」「ダウンロード」「外部リンク」を種別にすると、
**1 つのレッスンで複数を扱えなくなる**。

実際の既存教材は
「解説を読む → 素材をダウンロード → Canva で作る → 提出方法を確認する」
のように混在しているのが普通で、種別を切ると 1 つの教材が
4 レッスンに分割され、受講生の学習単位が壊れる。

**種別は「プレイヤーを出すか / 本文を出すか」の 2 択で足り、
中身の多様性はブロックで表す。**

---

## 7. 受講生側 UI の変更

### 流用できるもの（変更不要）

`/watch/[lessonId]` の**外枠はそのまま使える**。

| 要素 | 流用 |
| --- | --- |
| コース内カリキュラム（右カラム / タブ） | そのまま |
| 前へ / 次へ、通し番号ピル（`05 / 18`） | そのまま |
| 「視聴済みにする」ボタン、お気に入り | そのまま（文言だけ変える） |
| タグ（レベル・ツール・カテゴリ） | そのまま |
| Mobile の没入レイアウト・戻る矢印 | そのまま |
| コース一覧 / 動画一覧 / 検索 / 履歴 / お気に入りのカード | そのまま |

### 差し替えるのは中央の 1 ブロックだけ

```
【動画レッスン】                【テキスト / 課題レッスン】
┌────────────────┐            ┌────────────────┐
│  プレイヤー      │    →      │  本文（ブロック） │
└────────────────┘            └────────────────┘
   タイトル・タグ                  タイトル・タグ
   視聴済みにする                  学習完了にする
   この動画について                 （本文に統合）
   カリキュラム                    カリキュラム
```

つまり **`WatchView` の「映像ステージ」を出し分ける**だけで、
その周りは触らない。

### 文言の出し分け

`lesson_type` に応じて言い換える。**新しい画面は作らない。**

| 現在 | テキスト / 課題のとき |
| --- | --- |
| 視聴済みにする | 学習完了にする |
| この動画について | この教材について |
| 次の動画 | 次の教材 |
| 学習済み動画 4 本 | 学習済み 4 本 |
| 尺（12:45） | 目安 10 分 / 表示しない |

### 課題レッスンの見え方

専用画面は作らない。ブロックの並びで表現する。

```
callout(tone=task)「課題：バナーを 1 枚作る」
paragraph          手順の説明
image              完成イメージ
file               テンプレート.psd
link               Canva で開く
callout(tone=info) 提出方法
─────────────────
[ 学習完了にする ]
```

### 完了の扱い

- 動画：90% 自動完了 + 手動（現状のまま）
- テキスト / 課題：**手動のみ**
  - 自動化（最下部までスクロールで完了など）は初期には入れない。
    誤判定で「完了したのに戻る」が起きるほうが体験を損なう

---

## 8. 管理画面 CMS の変更

**Notion を再現しない。** Phase 7-G のお知らせエディタと同じ思想で、
**ブロックを足して並べ替えるだけ**の最小構成にする。

### レッスン編集画面に足すもの

```
レッスンの種別   ( ● 動画   ○ テキスト・課題 )

── 動画を選んだとき ──
  （現在の動画カード。アップロード・同期・状態表示）

── テキスト・課題を選んだとき ──
  本文
  ┌──────────────────────────────┐
  │ [見出し]   はじめに                  [↑][↓][削除] │
  │ [段落]     この教材では…              [↑][↓][削除] │
  │ [画像]     完成イメージ.png           [↑][↓][削除] │
  │ [ファイル] テンプレート.psd            [↑][↓][削除] │
  │ [リンク]   Canva で開く               [↑][↓][削除] │
  │ [課題]     バナーを 1 枚作る           [↑][↓][削除] │
  └──────────────────────────────┘
  [＋見出し] [＋段落] [＋箇条書き] [＋画像] [＋ファイル] [＋リンク] [＋課題]

  目安時間   [ 10 ] 分
```

### 流用できる実装

| 必要なもの | 流用元 |
| --- | --- |
| ブロックの追加・並べ替え・削除 | `AnnouncementEditor`（Phase 7-G）がほぼそのまま使える |
| 保存前の空ブロック除去 | `cleanBlocks()`（Phase 7-G） |
| 公開 / 下書きの切替 | `LessonFieldsForm`（Phase 7-H） |
| 並び替え | `moveLesson()`（Phase 7-H） |
| ファイルのアップロード UI | `VideoUploader`（Phase 7-E）の進捗表示を流用 |

**新規に作るのは「画像 / ファイルのアップロード先が Supabase Storage になる部分」だけ。**

### 運営が SQL を書かずに済む範囲

統合後、管理画面だけで完結するもの：

- レッスンの新規作成（種別の選択を含む）
- 本文の編集（見出し・段落・箇条書き・画像・ファイル・リンク・課題）
- 素材のアップロードと差し替え
- 並び替え・公開 / 非公開
- 目安時間の設定

SQL が残るのは Phase 7 と同じ 3 つ（**新しいコースの立ち上げ / カテゴリ追加 / 管理者の追加**）。

---

## 9. 既存教材の移行方法

### 推奨：**手作業 + 管理画面**（初回のみ）

自動移行スクリプトは**作らない**。

| 方式 | 評価 |
| --- | --- |
| **手作業（管理画面）** | **推奨。** 教材の整理・取捨選択が同時にできる。Notion の構造をそのまま持ち込まずに済む |
| Markdown を貼り付け | 補助として有用。段落ブロックに貼るだけ |
| Notion Export（HTML/MD） | 画像パスの張り替えが必要で、結局手作業が発生する |
| Notion API | 初回 1 回のために書くコストに見合わない |
| CSV | 本文がブロック構造なので相性が悪い |

### 判断の根拠

- 移行は**一度きり**。継続的な同期は不要（統合後はフリキャリが正）
- Notion の教材は「Notion の書き方」に最適化されているため、
  そのまま持ち込むとフリキャリ上で読みにくくなる
- **教材の棚卸しを兼ねられる**。古い記述・重複・不要ページを落とす好機

### 補助として作ってよいもの

本文が長い場合に限り、**Markdown → ブロック配列の変換**を管理画面に足すと楽になる。

```
[Markdown を貼り付けて変換]
  # 見出し    → heading
  段落        → paragraph
  - 箇条書き  → list
  ![](画像)   → image（アップロードは別途）
```

これは 1 日程度で作れて、移行作業を大幅に短縮できる。**画像とファイルは手動アップロードのまま**にする。

---

## 10. Phase 7 への影響

**後方互換を最優先する。既存の 90 本は 1 行も変更しない。**

| 機能 | 影響 | 対処 |
| --- | --- | --- |
| 動画視聴 | **なし** | `lesson_type = 'video'` が既定。分岐の外側は触らない |
| Cloudflare Stream | **なし** | 動画レッスンでしか使わない |
| 受講権限 / RLS | **なし** | コース単位。種別と無関係 |
| 学習進捗 | **小** | `position_seconds` はテキストでは常に 0。既存ロジックはそのまま動く |
| 完了判定 | **小** | 90% 自動完了は動画のみ。テキストは手動経路（実装済み）を使う |
| **コース進捗率** | **中** | 完了本数 ÷ 総本数。テキストも 1 本と数えれば整合する。**要確認** |
| **総学習時間の集計** | **中** | `duration_seconds` の合計。**テキストが 0 だと学習時間が伸びない** → 目安時間を入れる |
| 動画一覧 / 検索の絞り込み | **中** | ツール・尺での絞り込みにテキストが混ざる。**「動画のみ」フィルタの追加を検討** |
| サムネイルの尺表示 | **小** | 尺が 0 のとき「--:--」等にする |
| 管理画面 | **小** | レッスン一覧に種別列を足す。動画カードは種別が video のときだけ出す |
| Mobile 表示 | **小** | 没入レイアウトはプレイヤー前提。本文表示時はヘッダーを出す判断が要る |

### 絶対に守ること

1. **`lesson_type` の既定値は `video`。** migration で既存行を UPDATE しない
2. **プレイヤー周辺のコードを条件分岐で汚さない。**
   `WatchView` の中央だけを差し替え、`StreamStage` や `useIsDesktopLayout` には触れない
   （Phase 5・6 で最も壊れやすかった箇所）
3. **受講生向けの取得に公開条件を明示する原則を守る**（`FURICARI_HANDOFF.md` 参照）
4. **リンクは `LessonLink` を使う。** 種別が増えてもここを迂回しない

---

## 11. リスク

| # | リスク | 影響 | 対策 |
| --- | --- | --- | --- |
| 1 | プレイヤー周辺に分岐が増えて動画が壊れる | **大** | 中央 1 ブロックの出し分けに限定する。`StreamStage` は触らない。回帰テスト C1〜C4 を必ず通す |
| 2 | 総学習時間が伸びなくなる | 中 | テキストにも目安時間を持たせる。0 のまま運用しない |
| 3 | 「動画一覧」にテキストが混ざって意味が変わる | 中 | 名称を「教材一覧」に変えるか、種別フィルタを足す。**事業判断が要る**（→ 14 章） |
| 4 | Supabase Storage の容量・転送量 | 中 | 無料枠は 1GB。プロジェクトファイル等を配ると足りなくなる可能性。**事前に総量を見積もる** |
| 5 | 素材の URL が漏れる | 中 | 非公開バケット + 署名付き URL。受講権限をサーバー側で判定してから発行する |
| 6 | ブロック種別を増やしすぎて運営が使えなくなる | 中 | 7 種類までに抑える。「まず作って、足りなければ足す」 |
| 7 | 移行中に中途半端な教材が公開される | 小 | 下書きで作り、完成してから公開（Phase 7-H で実装済み） |
| 8 | Mobile の没入レイアウトが本文で使いにくい | 小 | テキストのときはヘッダーを出す |

---

## 12. 推奨実装 Phase

Phase 7 と同じく、**各段階で品質ゲート（テスト・型・Lint・本番ビルド・実ブラウザ）を通す**。
専用ブランチ `feature/phase9-legacy-content` を想定（Phase 8 の内容次第で番号は調整）。

### X-A｜DB とデータ層の拡張

- **目的**：本文を持てるようにする。既存を壊さない
- **内容**：`lesson_type` / `body` の 2 列追加、型定義、純粋関数（ブロックの検証・整形）とテスト
- **既存への影響**：なし（既定値 `video`）
- **難易度**：低 / **規模**：小
- **リスク**：ほぼ無い。この段階では画面に何も出ない

### X-B｜受講生側の本文表示

- **目的**：テキストレッスンが読める状態にする
- **内容**：`LessonBody` 部品（heading / paragraph / list / callout）、`WatchView` の中央出し分け、文言の出し分け
- **既存への影響**：**動画レッスンの見た目は変えない**（分岐の外側を触らない）
- **難易度**：中 / **規模**：中
- **リスク**：**最大の山**。プレイヤー周辺を壊さないこと。回帰テスト C を必ず通す

### X-C｜ファイル・画像の配布

- **目的**：素材をダウンロードできるようにする
- **内容**：Supabase Storage の非公開バケット、署名付き URL の発行（受講権限を確認してから）、`image` / `file` ブロックの描画
- **既存への影響**：なし（新規経路）
- **難易度**：中 / **規模**：中
- **リスク**：権限チェックの漏れ。**Phase 6-E と同じく「発行前に判定する」形にする**

### X-D｜管理画面のブロックエディタ

- **目的**：運営が SQL を書かずに教材を作れる状態にする
- **内容**：種別の選択、ブロックエディタ（`AnnouncementEditor` を流用）、画像・ファイルのアップロード、目安時間
- **既存への影響**：レッスン編集画面に追加。動画カードは種別が video のときだけ出す
- **難易度**：中 / **規模**：中〜大
- **リスク**：作り込みすぎ。**Notion を目指さない**

### X-E｜既存教材の移行

- **目的**：Notion の教材をフリキャリへ移す
- **内容**：手作業での投入。必要なら Markdown 貼り付け変換を補助として実装
- **既存への影響**：なし
- **難易度**：低（作業量は多い） / **規模**：教材量に比例
- **リスク**：中途半端な状態での公開 → 下書きで作る

### X-F｜総合 QA と本番反映

- **目的**：動画教材が壊れていないことの確認
- **内容**：`FURICARI_HANDOFF.md` の回帰テスト A〜E を全通し。特に **C（動画）** と **B（受講権限）**
- **難易度**：低 / **規模**：小

---

## 13. 工数感

| Phase | 規模 | 目安 |
| --- | --- | --- |
| X-A DB 拡張 | 小 | 0.5 日 |
| X-B 本文表示 | 中 | 1.5〜2 日 |
| X-C ファイル配布 | 中 | 1.5 日 |
| X-D 管理画面エディタ | 中〜大 | 2〜3 日 |
| X-E 移行（実装分） | 小 | 0.5〜1 日 |
| X-F 総合 QA | 小 | 0.5 日 |
| **開発計** | **中** | **6.5〜8.5 日** |
| X-E 移行（運用作業） | — | 教材量による。**開発とは別に見積もる** |

> 比較：Phase 7（管理画面一式）は同じ尺度で 8〜10 日相当だった。
> 今回はその 7〜8 割の規模で、**既存部品の流用率が高い分だけ軽い**。

---

## 14. 実装前に決める必要があること

**私（実装側）では決められない。事業判断が要る。**

| # | 決めること | 選択肢 | 影響 |
| --- | --- | --- | --- |
| 1 | **「動画一覧」の扱い** | (a) 名称を「教材一覧」に変える / (b) 動画のみのままにしてテキストは出さない / (c) 種別フィルタを足す | X-B の設計。ナビゲーションの名称も変わる |
| 2 | **既存教材の量** | レッスン換算で何本ぶんか / 素材ファイルの総容量 | X-E の作業量、Supabase Storage の容量プラン |
| 3 | **素材ファイルの置き場所** | (a) Supabase Storage / (b) 既存の Google Drive 等へリンク | (b) なら X-C が **不要**になり、`link` ブロックだけで済む |
| 4 | **課題の提出を受け付けるか** | (a) 提示のみ（推奨） / (b) 提出・添削まで | (b) なら別 Phase。規模が跳ね上がる |
| 5 | **テキスト教材の所要時間** | (a) 目安を手入力 / (b) 表示しない | 総学習時間の集計と、受講生の学習計画に影響 |
| 6 | **既存教材を残すか** | (a) Notion を畳んでフリキャリへ一本化 / (b) 併存 | (b) だと二重管理になる。**(a) を推奨** |

### 特に 3 番が効く

素材を **Google Drive のままリンクで配る**なら、**X-C（1.5 日）が丸ごと不要**になり、
Supabase Storage の容量リスクも消える。

一方で「受講権限が切れた受講生も Drive のリンクを開ける」状態になるため、
**素材を権限で守りたいかどうか**が判断の分かれ目になる。

---

## 付録：この調査で確認した事実

| 確認したこと | 結果 |
| --- | --- |
| 過去の統合調査資料 | **存在しない**（今回が新規調査） |
| `stream_video_id` の nullable | **null 可。90 本中 89 本が null** |
| 動画無しレッスンの公開 | `canPublishLesson` が既に許可している |
| 手動完了の経路 | 実装済み（「視聴済みにする」） |
| `duration_seconds` の利用箇所 | 10 ファイル以上（尺表示・集計・絞り込み） |
| ブロック配列の実績 | `announcements.body` で稼働中（Phase 6〜7） |
| 流用できるエディタ | `AnnouncementEditor` / `LessonFieldsForm` / `VideoUploader` |

**参照したファイル**

- `FURICARI_HANDOFF.md`（Phase 7 完成版の現況・回帰テスト）
- `PHASE7_ADMIN_SPEC.md`（管理画面の設計判断）
- `supabase/migrations/20260819140000_create_content_tables.sql`（lessons の定義）
- `supabase/migrations/20260822130000_add_lesson_stream_status.sql`
- `src/lib/stream/video-state.ts`（`canPublishLesson`）
- `src/components/video/WatchView.tsx`（受講生側の構成）
- `src/app/api/progress/route.ts`（完了判定）
- `src/lib/admin/announcement-draft.ts` / `src/components/admin/AnnouncementEditor.tsx`（流用元）
