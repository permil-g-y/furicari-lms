# 既存教材統合 調査・設計書

> **Notion 実物確認済み：YES**
>
> 確認日: 2026-08-25 / 対象: 「フリキャリ受講生専用ページ」（ゲスト権限で閲覧）
> LMS 側: **Phase 7 完成版**（`main` = Vercel Production）
> ステータス: **調査・設計のみ。コード変更・migration・Supabase 変更は未実施**

---

## 0. 実物調査のサマリ（先に結論だけ）

| | |
| --- | --- |
| 実際に開いたか | **YES**（トップ → 授業課題一覧 → 個別課題 → Tips 一覧 → 学習の進め方） |
| トップ直下のセクション | **7** |
| 課題（教材本体） | **27**（Pr 11 / Ps 5 / Ae 10 / 営業関連 1） |
| Tips（補助教材） | **71**（Premiere Pro 33 / After Effects 29 / その他 9） |
| FAQ | 6 以上（運営関連 2 / 講師 2 / 休会・退会 2） |
| **総ページ数（概算）** | **110〜130** |
| 1 課題あたりの本文量 | **約 2,300 文字** |
| 1 課題あたりの画像 | **約 26 枚**（操作手順のスクリーンショット） |
| **画像総数（概算）** | **500〜700 枚** |
| 動画 | **Vimeo 埋め込み**（完成見本）。Cloudflare Stream ではない |
| 素材配布 | **Google Drive**（ZIP をダウンロード） |
| 課題提出 | **講師へ提出**（Notion 上に提出機能は無い。運用で回している） |

### この実物調査で、前回の設計判断のうち **2 つが覆りました**

| 前回の判断 | 実物確認後 | 理由 |
| --- | --- | --- |
| 「手作業＋管理画面」で移行 | **❌ 覆る → Markdown 変換の実装が必須** | 1 課題 2,300 文字 × 27 本 + Tips 71 本。手打ちは現実的でない |
| Supabase Storage が必要 | **❌ 覆る → 初期は不要** | 素材は既に Google Drive で配布中。**リンクのままで足りる** |
| 課題提出機能は不要 | ✅ 妥当（実物でも Notion 側に提出機能は無い） | |
| `lesson_type` = video / article の 2 値 | ✅ 妥当（後述のとおり **3 値に微修正**） | |
| 本文は jsonb のブロック配列 | ✅ 妥当 | |
| ブロック 7 種類 | ✅ ほぼ妥当（**画像が主役**である点だけ設計に反映） | |

---

## 1. 結論

### 統合を推奨する。ただし **画像の移行が全体の山場**である。

実物を見て分かった最大の事実は、**この教材はテキストではなく「スクリーンショット付きの操作手順書」である**こと。

1 課題ページの実際の構成（「ショート動画編集①」を実測）：

```
◆ 課題の概要        … 2 段落
◆ 課題のゴール      … 3 段落（提出要否の注記を含む）
完成動画            … Vimeo 埋め込み
Tips へのリンク     … Notion 内部ページへの参照
素材のダウンロード  … Google Drive へのリンク（ZIP）
編集の準備
① テロップを入れる  … 手順文 + スクリーンショット多数
② …                 … 同上
③ テロップの作成    … 同上
④ 動画を書き出し    … 同上
💡 もっと知りたい人は… … callout
◆ 編集する際の注意点 … 箇条書き
```

**本文 2,339 文字に対して画像 26 枚。** 文章より画像のほうが情報量として重い。

### 推奨する形（実物を踏まえて修正）

**`lessons` に `lesson_type` と `body`（jsonb ブロック配列）を足す。** ここは前回のまま。

ただし実物を見て 3 点を修正する。

| # | 修正 | 理由 |
| --- | --- | --- |
| 1 | `lesson_type` を **`video` / `article` / `assignment`** の 3 値にする | 実物では「課題」と「Tips（読み物）」が明確に別物として運用されている。一覧での見分け・絞り込みに種別が要る |
| 2 | **Supabase Storage は初期スコープから外す** | 素材は Google Drive のまま。`link` ブロックで足りる |
| 3 | **Markdown → ブロック変換を必須機能にする**（補助ではなく本体） | 教材量が手作業の限界を超えている |

### 実装規模：**中〜大**（前回「中」から上方修正）

開発そのものは中規模だが、**移行作業（画像 500〜700 枚）が大きい**。

---

## 2. 現在の LMS 構造

### 階層

```
Course（6）→ Chapter（25）→ Lesson（90）
```

### `lessons` の主要列（Phase 7 時点）

| 列 | 備考 |
| --- | --- |
| `slug` | URL（`/watch/premiere-practice-05`） |
| `title` / `description` | 説明は 1 行 |
| `key_points` (jsonb) | 「学べること」の箇条書き |
| **`stream_video_id` (null 可)** | **90 本中 89 本が null のまま公開されている** |
| `duration_seconds` | 尺。10 ファイル以上で使用（表示・集計・絞り込み） |
| `is_published` / `sort_order` / `number` | |
| `stream_status` / `stream_synced_at` / `stream_error` | Phase 7-E で追加 |

### Lesson はどこまで動画前提か

**DB は動画前提ではない。UI と文言が動画前提。**

Phase 7 の時点で既にこう書かれている。

```ts
// src/lib/stream/video-state.ts
export function canPublishLesson(lesson) {
  if (!lesson.streamVideoId) return { ok: true };   // テキスト教材を締め出さない
  ...
}
```

| 層 | 動画依存 |
| --- | --- |
| DB / 受講権限 / 公開制御 / 並び替え | **なし** |
| 進捗・完了判定 | 中（90% 自動完了は再生位置ベース。**手動完了の経路は実装済み**） |
| 受講生 UI | 高（プレイヤーが主役。動画が無いとダミープレイヤーが出る） |
| 文言 | 高（「この動画について」「視聴済みにする」「次の動画」） |

---

## 3. 既存教材との違い（実物ベース）

### 実際に存在した教材形式

| 形式 | 実例 | 件数 |
| --- | --- | --- |
| **操作手順書（画像多数）** | ショート動画編集①〜、YouTube 定点編集①〜③ | 課題 27 本の大半 |
| **読み物（Tips）** | 「よく使うエフェクト・トランジション一覧」 | 71 |
| **完成見本の動画** | Vimeo 埋め込み | 課題ごとに 1 本程度 |
| **素材配布** | Google Drive の ZIP | 課題ごとに 1 本程度 |
| **内部リンク** | 課題 → 関連 Tips への参照 | 多数 |
| **callout（💡ヒント）** | 「もっと知りたい人は…」 | 各ページに複数 |
| **データベース（ボード / テーブル）** | 授業課題一覧、Tips 一覧、FAQ | 3 以上 |
| **プロパティ** | パターン（A/B/C）、難易度（★1〜5）、ツール（Pr/Ps/Ae） | 課題データベース |
| **カレンダー** | 「直近のイベント」 | トップ |
| **FAQ（Q&A 形式）** | 休会・退会など | 6 以上 |

### 存在しなかった / 確認できなかった形式

| 形式 | 状況 |
| --- | --- |
| Cloudflare Stream の動画 | **無い**（動画は Vimeo） |
| 課題の提出フォーム | **無い**（「講師へ提出」と書かれているだけ。提出は Notion 外の運用） |
| チェックリスト（To-do ブロック） | 確認した範囲では**見当たらなかった** |
| 数式・コードブロック | 同上 |
| Notion テンプレートボタン | 同上 |
| PDF の直接添付 | **確認できなかった**（配布は Google Drive 経由） |

> 上記「確認できなかった」は、**全 110〜130 ページを網羅していないため断定できない**。
> 実際に開いたのは 5 ページ（トップ / 授業課題一覧 / 個別課題 1 / Tips 一覧 / 学習の進め方）。

### LMS と噛み合わない点（実物で確定したもの）

1. **本文と画像を置く場所が無い** … `description` は 1 行のみ
2. **完了判定が再生位置に依存** … テキストには再生位置が無い（手動経路で解決可能）
3. **動画が Vimeo にある** … Cloudflare Stream ではない
4. **素材が Google Drive にある** … 受講権限では守られていない
5. **課題のメタ情報（パターン・難易度）を持つ列が無い**

---

## 4. 現状のままで対応できること

追加開発なしで**今日から**できること。

| できること | 根拠 |
| --- | --- |
| 動画を持たないレッスンを作って公開する | `stream_video_id` は null 可。`canPublishLesson` が許可済み |
| コース / チャプターに混在させる | 種別の概念が無いため |
| 受講権限で保護する | コース単位（Phase 6-E） |
| 並び替え・公開切替 | Phase 7-H の管理画面 |
| 検索に載せる | タイトル・説明・学べること・チャプター名・コース名が対象 |
| お気に入り / 履歴 / 進捗 | 動画かどうかを見ていない |

### ただし実物の教材は、これだけでは移せない

本文 2,300 文字と画像 26 枚を `description`（1 行）と `key_points`（箇条書き）に押し込むことはできない。
**「今のままで移せる」のは Tips の一部（短い読み物）だけ。**

---

## 5. 追加で必要な機能（実物を踏まえて優先度を再設定）

| # | 機能 | 必須度 | 実物での根拠 |
| --- | --- | --- | --- |
| 1 | 本文（ブロック配列）の列 | **必須** | 2,300 文字の手順書を置く場所が無い |
| 2 | **画像ブロック + 画像ホスティング** | **必須** | 1 課題 26 枚。教材の主役 |
| 3 | レッスン種別（3 値） | **必須** | 課題 / Tips / 動画の区別が運用上ある |
| 4 | 本文の描画（受講生側） | **必須** | |
| 5 | **Markdown → ブロック変換** | **必須（前回は「補助」）** | 教材量が手作業の限界を超えている |
| 6 | 本文のエディタ（管理画面） | **必須** | 運営が SQL を書かずに済むこと |
| 7 | 外部リンクブロック | **必須** | Google Drive・Vimeo・内部参照 |
| 8 | 課題のメタ（パターン / 難易度） | **推奨** | 実物にあり、受講生の選択基準になっている |
| 9 | 所要時間の手入力 | **推奨** | 集計を壊さないため（→ 10 章） |
| 10 | Supabase Storage でのファイル配布 | **初期は不要** | 素材は Google Drive のまま |
| 11 | 課題の提出機能 | **不要** | Notion 側にも無い。運用で回っている |

### 画像のホスティングだけは避けて通れない

Notion の画像 URL は**署名付きで期限がある**ため、そのまま `<img src>` にはできない。
選択肢は 2 つ。

| 案 | 内容 | 評価 |
| --- | --- | --- |
| **A. Supabase Storage** | 画像を落として公開バケットへ上げ直す | **推奨。** 同一プロジェクト内で完結。500〜700 枚でも数百 MB 程度 |
| B. 外部（Cloudflare Images 等） | 別サービスを契約 | 契約が増える。現時点では不要 |

> **前回「Storage は素材配布のために必要」と書いたが、実際は「画像のために必要」だった。**
> 素材（ZIP）は Drive のままでよく、**画像は移す必要がある**。用途が入れ替わる。

---

## 6. 推奨データ構造

### 変更は 2 列（前回と同じ。種別だけ 3 値へ）

```sql
-- 案（未適用）
create type public.lesson_type as enum ('video', 'article', 'assignment');

alter table public.lessons
  add column if not exists lesson_type public.lesson_type not null default 'video',
  add column if not exists body jsonb not null default '[]'::jsonb;
```

**`default 'video'` により既存 90 本は 1 行も変更されない。**

### 課題のメタ情報

パターン（A/B/C）と難易度（★1〜5）は**列を足さず `body` の外に置かない**。
`key_points` と同じ考え方で、**専用の 2 列**にするのが素直。

```sql
-- 任意（課題を扱うなら）
alter table public.lessons
  add column if not exists assignment_pattern text,      -- 'A' | 'B' | 'C'
  add column if not exists assignment_level integer;     -- 1〜5（★の数）
```

一覧での絞り込みに使うため、jsonb ではなく列にする。

### 本文ブロックの型（実物に合わせて確定）

```ts
export type LessonBlock =
  | { type: "heading";   text: string }
  | { type: "paragraph"; text: string }
  | { type: "list";      title?: string; items: string[] }
  | { type: "image";     path: string; alt: string; caption?: string }  // ← 最頻出
  | { type: "callout";   tone: "info" | "task" | "warn"; title: string; body: string }
  | { type: "link";      href: string; label: string; note?: string }   // Drive / Vimeo / 内部
  | { type: "embed";     provider: "vimeo"; id: string; title?: string };
```

- **`image` が最頻出**。エディタも描画も、まずここを作り込む
- `file` ブロックは**作らない**（Drive リンクで足りる）。必要になったら足す
- `embed` は当面 Vimeo のみ。プロバイダを増やせる形にしておく

### なぜ別テーブルにしないのか（前回と同じ結論）

| | jsonb 1 列 | 別テーブル |
| --- | --- | --- |
| 取得 | 既存の 1 回のクエリに含まれる | join か 2 回目のクエリ |
| 並び替え | 配列の順序そのもの | `sort_order` の管理が必要 |
| ブロック種別の追加 | migration 不要 | 列追加や型変更 |

教材が 200 ページ規模でも、1 リクエストで全件をメモリに載せる現在の方式で問題ない。
**本文の全文検索が必要になったら**、そのとき `body` から生成列を作る。

---

## 7. 受講生側 UI の変更

### 流用できるもの（変更不要）

`/watch/[lessonId]` の**外枠はそのまま使える**。

| 要素 | 流用 |
| --- | --- |
| コース内カリキュラム（右カラム / タブ） | そのまま |
| 前へ / 次へ、通し番号ピル（`05 / 18`） | そのまま |
| 「視聴済みにする」・お気に入り | そのまま（文言のみ変更） |
| タグ（レベル・ツール・カテゴリ） | そのまま |
| Mobile の没入レイアウト・戻る矢印 | **要調整**（本文にはヘッダーが要る） |
| 一覧・検索・履歴・お気に入りのカード | そのまま |

### 差し替えるのは中央の 1 ブロックだけ

```
【動画レッスン】             【記事 / 課題レッスン】
┌──────────────┐            ┌──────────────┐
│  プレイヤー    │    →      │  本文（ブロック）│
└──────────────┘            └──────────────┘
   タイトル・タグ                タイトル・タグ・難易度
   視聴済みにする                学習完了にする
   この動画について               （本文に統合）
   カリキュラム                  カリキュラム
```

### 実物の課題ページを、そのままブロックで再現できる

```
heading   「課題の概要」
paragraph  YouTube のショート動画のテロップを…
heading   「課題のゴール」
paragraph  できるだけ完成動画に近づけること。
callout(info) この課題は練習用です。提出は不要ですが…
embed(vimeo) 完成動画
link       関連 Tips（内部レッスンへ）
link       素材のダウンロード（Google Drive）
heading   「① テロップを入れる」
paragraph  手順の説明
image      操作画面のスクリーンショット
...（②③④ が続く）
callout(info) 💡 もっと知りたい人は…
list       編集する際の注意点
──────────────
[ 学習完了にする ]
```

**新しい画面は要らない。ブロックの並びで再現できる。**

### 文言の出し分け

| 現在 | 記事 / 課題のとき |
| --- | --- |
| 視聴済みにする | 学習完了にする |
| この動画について | この教材について |
| 次の動画 | 次の教材 |
| 学習済み動画 4 本 | 学習済み 4 本 |
| 尺（12:45） | 目安 15 分 / 非表示 |

### 完了の扱い

- 動画：90% 自動完了 + 手動（現状のまま）
- 記事 / 課題：**手動のみ**。スクロール完了などの自動判定は入れない
  （誤判定で「完了したのに戻る」ほうが体験を損なう）

---

## 8. 管理画面 CMS の変更

**Notion を再現しない。** Phase 7-G のお知らせエディタと同じ思想。

### レッスン編集画面に足すもの

```
種別    ( ● 動画   ○ 記事   ○ 課題 )

── 課題を選んだとき ──
  パターン [ A ▾ ]   難易度 [ ★★★ ]   目安時間 [ 30 ] 分

── 記事 / 課題のとき ──
  本文
  ┌──────────────────────────────┐
  │ [見出し] ① テロップを入れる         [↑][↓][削除] │
  │ [段落]   ツールバーの「T」を…       [↑][↓][削除] │
  │ [画像]   操作画面.png               [↑][↓][削除] │
  │ [リンク] 素材のダウンロード          [↑][↓][削除] │
  │ [埋込]   Vimeo 完成動画             [↑][↓][削除] │
  └──────────────────────────────┘
  [＋見出し][＋段落][＋箇条書き][＋画像][＋リンク][＋ヒント][＋埋込]

  ┌─ Markdown を貼り付けて変換 ────────┐
  │ （移行用。貼るとブロックへ分解される）   │
  └────────────────────────────┘
```

### 流用できる実装

| 必要なもの | 流用元 |
| --- | --- |
| ブロックの追加・並べ替え・削除 | `AnnouncementEditor`（7-G）がほぼそのまま |
| 保存前の空ブロック除去 | `cleanBlocks()`（7-G） |
| 公開 / 下書きの切替・並び替え | `LessonFieldsForm` / `moveLesson()`（7-H） |
| アップロードの進捗表示 | `VideoUploader`（7-E） |

**新規に作るのは「画像アップロード（Supabase Storage）」と「Markdown 変換」だけ。**

### 画像アップロードは一括対応が要る

1 課題 26 枚を 1 枚ずつ選ばせると運営が壊れる。**複数選択で一括アップロード → 本文へ順に挿入**できる形にする。

---

## 9. 既存教材の移行方法（**前回の結論を覆す**）

### 前回：「手作業＋管理画面」 → 今回：**「Notion Export + Markdown 変換」**

### 覆した理由（実測値）

| | 実測 | 手作業だと |
| --- | --- | --- |
| 課題 | 27 本 × 約 2,300 文字 | 約 62,000 文字の手打ち |
| Tips | 71 本 | さらに数万文字 |
| 画像 | 1 課題 26 枚 → **総計 500〜700 枚** | 1 枚ずつ保存・アップロード・挿入 |

**手作業では非現実的。** 前回はこの量を知らずに「手作業が合理的」と書いていた。

### 推奨する移行手順

```
1. Notion を Markdown & CSV でエクスポート（画像も同梱される）
       ↓
2. 画像を Supabase Storage へ一括アップロード（スクリプト）
       ↓
3. Markdown の画像パスを Storage のパスへ置換
       ↓
4. 管理画面の「Markdown を貼り付けて変換」で 1 レッスンずつ投入
       ↓
5. 人が見て整える（見出しの粒度・不要な記述の削除・リンクの張り直し）
       ↓
6. 下書きのまま確認 → 公開
```

**3 までは自動、4 以降は人の判断**という分け方にする。
全自動の移行システムは作らない（教材の棚卸しを兼ねたいため）。

### 各方式の再評価

| 方式 | 前回の評価 | 実物確認後 |
| --- | --- | --- |
| 手作業のみ | **推奨** | **❌ 非現実的**（画像 500〜700 枚） |
| **Notion Export（MD + 画像）** | 「張り替えが必要」 | **✅ 推奨。** 画像が一括で手に入るのが決定的 |
| Markdown 貼り付け変換 | 「補助」 | **✅ 必須機能へ格上げ** |
| Notion API | 「割に合わない」 | ✅ 変わらず不要（Export で足りる） |
| CSV | 相性が悪い | ✅ 変わらず不要。**ただし課題データベースのプロパティ（パターン / 難易度）取得には有用** |

---

## 10. Phase 7 への影響

**後方互換を最優先。既存 90 本は 1 行も変更しない。**

| 機能 | 影響 | 対処 |
| --- | --- | --- |
| 動画視聴 / Cloudflare Stream | **なし** | `lesson_type = 'video'` が既定 |
| 受講権限 / RLS | **なし** | コース単位 |
| 学習進捗 | 小 | `position_seconds` は記事では 0 のまま |
| 完了判定 | 小 | 90% 自動完了は動画のみ。手動経路は実装済み |
| コース進捗率 | 中 | 記事も 1 本と数えれば整合する。**要確認** |
| **総学習時間** | **中** | `duration_seconds` の合計。記事が 0 だと伸びない → **目安時間を持たせる** |
| 動画一覧 / 検索の絞り込み | **中** | 「動画一覧」に記事が混ざる。**名称と絞り込みの方針が要る（→ 14 章）** |
| サムネイルの尺表示 | 小 | 0 のとき非表示にする |
| 管理画面 | 小 | 一覧に種別列。動画カードは種別が video のときだけ |
| **Mobile 表示** | **中** | 没入レイアウトはプレイヤー前提。本文ではヘッダーを出す |

### 絶対に守ること

1. **`lesson_type` の既定値は `video`。** migration で既存行を UPDATE しない
2. **プレイヤー周辺を条件分岐で汚さない。** `StreamStage` / `useIsDesktopLayout` には触れない
   （Phase 5・6 で最も壊れやすかった箇所）
3. **受講生向けの取得は公開条件をクエリで明示する**（`FURICARI_HANDOFF.md` 参照）
4. **リンクは `LessonLink` を使う**

---

## 11. リスク

| # | リスク | 影響 | 対策 |
| --- | --- | --- | --- |
| 1 | プレイヤー周辺に分岐が増えて動画が壊れる | **大** | 中央 1 ブロックの出し分けに限定。回帰テスト C を必ず通す |
| 2 | **画像の移行が終わらない** | **大** | 500〜700 枚。Export + スクリプトを前提にする。手作業では詰む |
| 3 | **Notion の画像 URL が期限切れになる** | **大** | 署名付き URL をそのまま参照しない。**必ず自前へコピーする** |
| 4 | Supabase Storage の容量 | 中 | 無料枠 1GB。画像 700 枚なら数百 MB で収まる見込みだが**事前に実測する** |
| 5 | 素材（Drive）が受講権限で守られない | 中 | 現状も同じ。**統合しても悪化はしない**が、守りたいなら別途対応 |
| 6 | Vimeo と Cloudflare Stream の二重管理 | 中 | 当面は Vimeo 埋め込みのまま。将来 Stream へ寄せるかは別判断 |
| 7 | 総学習時間が伸びなくなる | 中 | 記事にも目安時間を持たせる |
| 8 | 「動画一覧」の意味が変わる | 中 | 名称変更か種別フィルタ。**事業判断（→ 14 章）** |
| 9 | 移行中に中途半端な教材が公開される | 小 | 下書きで作る（7-H で実装済み） |
| 10 | ブロック種別を増やしすぎる | 小 | 7 種類までに抑える |

---

## 12. 推奨実装 Phase

各段階で品質ゲート（テスト・型・Lint・本番ビルド・実ブラウザ）を通す。

### X-A｜DB とデータ層の拡張

- **目的**：本文と種別を持てるようにする。既存を壊さない
- **内容**：`lesson_type` / `body`（＋任意で `assignment_pattern` / `assignment_level`）、型定義、ブロックの検証・整形の純粋関数とテスト
- **既存への影響**：なし（既定値 `video`）
- **難易度**：低 / **規模**：小 / **リスク**：ほぼ無い

### X-B｜受講生側の本文表示

- **目的**：記事・課題が読める状態にする
- **内容**：`LessonBody` 部品（heading / paragraph / list / callout / image / link / embed）、`WatchView` の中央出し分け、文言の出し分け、Mobile のヘッダー調整
- **既存への影響**：動画レッスンの見た目は変えない
- **難易度**：中 / **規模**：中
- **リスク**：**最大の山**。回帰テスト C を必ず通す

### X-C｜画像ホスティングと移行スクリプト

- **目的**：500〜700 枚の画像を扱える状態にする
- **内容**：Supabase Storage のバケット、一括アップロード、Markdown の画像パス置換、管理画面からの複数枚アップロード
- **既存への影響**：なし（新規経路）
- **難易度**：中 / **規模**：中
- **リスク**：容量の見積もり。**先に実測する**

### X-D｜管理画面のブロックエディタ + Markdown 変換

- **目的**：運営が SQL を書かずに教材を作れる／移行できる
- **内容**：種別選択、ブロックエディタ（`AnnouncementEditor` 流用）、Markdown 貼り付け変換、課題メタの入力
- **既存への影響**：レッスン編集画面に追加
- **難易度**：中 / **規模**：中〜大
- **リスク**：作り込みすぎ。**Notion を目指さない**

### X-E｜既存教材の移行（運用作業）

- **目的**：Notion の教材をフリキャリへ移す
- **内容**：Export → 画像アップロード → Markdown 投入 → 人が整える → 公開
- **難易度**：低（作業量が多い） / **規模**：**課題 27 + Tips 71 = 約 100 ページ**
- **リスク**：中途半端な公開 → 下書きで作る

### X-F｜総合 QA と本番反映

- **内容**：`FURICARI_HANDOFF.md` の回帰テスト A〜E を全通し。特に **C（動画）** と **B（受講権限）**
- **難易度**：低 / **規模**：小

---

## 13. 工数感（実物確認後に上方修正）

| Phase | 規模 | 目安 | 前回 |
| --- | --- | --- | --- |
| X-A DB 拡張 | 小 | 0.5 日 | 0.5 日 |
| X-B 本文表示 | 中 | **2〜2.5 日** | 1.5〜2 日 |
| X-C 画像ホスティング + 移行スクリプト | 中 | **2 日** | 1.5 日 |
| X-D 管理画面 + Markdown 変換 | 中〜大 | **3〜4 日** | 2〜3 日 |
| X-F 総合 QA | 小 | 0.5 日 | 0.5 日 |
| **開発計** | **中〜大** | **8〜9.5 日** | 6.5〜8.5 日 |
| **X-E 移行（運用作業）** | — | **約 100 ページ。1 ページ 15〜30 分として 25〜50 時間** | 「教材量による」 |

**開発より移行作業のほうが重い。** ここが前回見えていなかった最大の差分。

---

## 14. 実装前に決める必要があること

| # | 決めること | 選択肢 | 影響 |
| --- | --- | --- | --- |
| 1 | **移行する範囲** | (a) 課題 27 のみ / (b) 課題 + Tips 71 / (c) 全部（FAQ・運営情報含む） | **X-E の作業量が 2〜3 倍変わる。最重要** |
| 2 | **「動画一覧」の扱い** | (a) 「教材一覧」に改称 / (b) 動画のみ維持 / (c) 種別フィルタを足す | X-B の設計とナビ名称 |
| 3 | **Vimeo をどうするか** | (a) 埋め込みのまま / (b) Cloudflare Stream へ移す | (b) なら動画の再アップロードが必要。**当面 (a) を推奨** |
| 4 | **素材（Google Drive）** | (a) リンクのまま / (b) Supabase Storage へ移す | **(a) を推奨。**(b) は容量と工数が増える |
| 5 | **課題のパターン / 難易度を残すか** | (a) 残す（列を追加） / (b) 捨てる | (a) なら X-A に列 2 本 |
| 6 | **Notion を畳むか** | (a) フリキャリへ一本化 / (b) 併存 | **(a) を推奨。**(b) は二重管理 |
| 7 | **課題の提出** | (a) 提示のみ（現状どおり） / (b) 提出機能を作る | (b) は別 Phase。規模が跳ね上がる |

### 特に 1 番で工数が決まる

- **(a) 課題 27 のみ** … 移行 7〜14 時間。まずここから始めるのが現実的
- **(b) + Tips 71** … 移行 25〜50 時間
- **(c) 全部** … さらに増える。FAQ や運営情報は**LMS ではなくお知らせ機能**で足りる可能性が高い

**推奨：(a) から始めて、運用しながら (b) を進める。**

---

## 付録 A：今回の実物調査の範囲

### 実際に開いたページ

| # | ページ | 確認した内容 |
| --- | --- | --- |
| 1 | フリキャリ受講生専用ページ（トップ） | 7 セクション構成、カレンダー、FAQ データベース、連絡先 |
| 2 | 📚 授業課題一覧 | ボードビュー。Pr 11 / Ps 5 / Ae 10 / 営業関連 1 = **27** |
| 3 | ショート動画編集①（個別課題） | 本文 2,339 字、**画像 26 枚**、Vimeo 1、Google Drive 1、callout 複数 |
| 4 | 🔆 Tips 一覧 | テーブルビュー。Pr 33 / Ae 29 / その他 9 = **71** |
| 5 | 📝【必ずお読みください】学習の進め方 | 画像 8、内部リンク多数、コース別の概要ページへ分岐 |

### 確認できなかったこと（**推測で埋めていない**）

| # | 未確認 | 影響 |
| --- | --- | --- |
| 1 | **課題 27 本すべての中身** | 開いたのは 1 本。**画像枚数の平均は 1 サンプルからの推定** |
| 2 | **Tips 71 本すべての中身** | 1 本も開いていない。読み物中心と推定 |
| 3 | セミナー / 勉強会、コーチング期間の動き、運営からのお知らせ | 未訪問 |
| 4 | 画像の総容量 | **未実測。Storage の容量判断に必要** |
| 5 | Notion のエクスポート可否 | **ゲスト権限のため未確認。オーナー権限が要る** |
| 6 | PDF の直接添付・チェックリスト・コードブロック等 | 見た範囲では無かったが、全ページ未確認 |

### 権限の制約

閲覧は**ゲスト権限**で行った。ページの閲覧はできたが、
**エクスポート機能の利用可否は確認できていない**（移行方式の前提になるため、要確認）。

---

## 付録 B：参照した LMS 側のファイル

- `FURICARI_HANDOFF.md`（Phase 7 完成版の現況・回帰テスト）
- `PHASE7_ADMIN_SPEC.md`（管理画面の設計判断）
- `supabase/migrations/20260819140000_create_content_tables.sql`（lessons の定義）
- `supabase/migrations/20260822130000_add_lesson_stream_status.sql`
- `src/lib/stream/video-state.ts`（`canPublishLesson`）
- `src/components/video/WatchView.tsx`（受講生側の構成）
- `src/app/api/progress/route.ts`（完了判定）
- `src/lib/admin/announcement-draft.ts` / `src/components/admin/AnnouncementEditor.tsx`（流用元）
