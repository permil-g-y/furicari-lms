# Phase 7 設計 — 管理画面・運用機能

> 作成: 2026-08-21 / 対象コミット: Phase 6 完了時点（`b223474`）
> ステータス: **設計のみ。コード変更・migration・Supabase 変更は未実施**

---

## 0. この Phase のゴール

**高機能な管理システムを作ることではない。**

> 非エンジニアの運営スタッフが、Supabase と Cloudflare を一度も開かずに
> フリキャリの日常業務を回せる状態にすること。

この 1 行が、以下すべての優先度判断の根拠になっている。

### 「日常業務」と「非日常業務」を分ける

判断基準は**発生頻度**。毎週起きることは画面へ、数ヶ月に一度のことは SQL のままでよい。

| 業務 | 頻度 | 現在のやり方 |
| --- | --- | --- |
| 新しい受講生を招待する | 週 | Supabase Dashboard |
| 受講コースを付与する | 週 | SQL Editor |
| 「動画が見られません」の調査 | 週 | SQL Editor |
| 新しい動画を追加する | 週 | Cloudflare Dashboard + SQL Editor |
| 受講期限を延長・停止する | 月 | SQL Editor |
| お知らせを出す | 月 | SQL Editor |
| レッスンの並び替え・公開切替 | 月 | SQL Editor |
| 学習状況を確認する | 月 | SQL Editor |
| 新しいコースを立ち上げる | 数ヶ月 | SQL Editor |
| カテゴリ・ツールを増やす | 数ヶ月 | SQL Editor |
| 管理者を増やす | ほぼ無し | SQL Editor |

---

## 1. いちばん重要な前提 — DB 側はもう Phase 7 の準備ができている

Phase 1 の設計時点で、**全テーブルに管理者用の RLS ポリシーが入っている**。

```
Admins manage courses / chapters / lessons / categories / tools
Admins manage enrollments
Admins manage announcements
```

`is_admin()`（SECURITY DEFINER）も `profiles.role` の昇格防止（列レベル権限 + BEFORE UPDATE トリガ）も実装済み。

**つまり Phase 7 は「DB を作り直す仕事」ではなく「既にある権限の上に画面を載せる仕事」**である。
新規の migration は最小限で済む（→ 6 章）。

---

## 2. 機能一覧と 3 段階の優先度

### 🔴 Phase 7 で絶対必要（これが無いと運営を開始できない）

| # | 機能 | 理由 |
| --- | --- | --- |
| 1 | 管理画面のアクセス制御 | 土台。これ無しに何も置けない |
| 2 | 受講生一覧・受講生詳細 | 問い合わせ対応の起点 |
| 3 | 受講コースの付与・解除 | **最頻業務**。今は毎回 SQL |
| 4 | 受講期限の設定・延長 | 課金と直結する |
| 5 | 受講生の招待（コース付与と同時） | 招待と付与が別作業だと必ず片方を忘れる |
| 6 | Cloudflare へのアップロード | 残り 89 本を入れるのに必須 |
| 7 | Video UID の自動紐付け | 手作業のコピペは必ず事故る |
| 8 | 実尺の自動取得（`duration_seconds`） | 現在の不一致がここで解消する |
| 9 | お知らせの作成・公開 | 受講生への唯一の連絡手段 |
| 10 | ダッシュボード（要対応の可視化） | 「UID 未設定のレッスン」等に気づけない |
| 11 | Custom SMTP | **招待メールが届かないとサービスが始まらない**（→ 5 章） |

### 🟡 できれば欲しい（無くても運営は回る）

| # | 機能 | 代替手段 |
| --- | --- | --- |
| 12 | レッスンの編集・並び替え・公開切替 | SQL（月 1 回程度） |
| 13 | 学習状況の分析（レッスン別の完了率） | SQL |
| 14 | Chapter の追加・改名 | SQL |
| 15 | コースの新規作成 | SQL（数ヶ月に一度） |
| 16 | 受講権限の一括付与（複数人 × 複数コース） | 1 人ずつ付与 |
| 17 | お知らせのプレビュー | 公開前に非公開で確認 |
| 18 | Cloudflare Webhook での自動同期 | 手動の「情報を取得」ボタン |

### ⬜ 後回しでいい

| # | 機能 | 理由 |
| --- | --- | --- |
| 19 | カテゴリ・ツールの管理 | グラデーション等のデザイン値を含み、画面化の価値が低い |
| 20 | CSV エクスポート | 需要が出てから |
| 21 | 学習状況のグラフ・時系列 | 数字が読めれば運営判断はできる |
| 22 | 監査ログ | 運営が 1〜2 名のうちは不要 |
| 23 | 受講生ごとの個別お知らせ | Phase 6 で全体配信のみと決定済み |
| 24 | **管理者の追加（role 変更）** | **あえて画面に置かない**（→ 3 章） |

---

## 3. 管理者ログイン・アクセス制御

### 方針

**受講生と同じ `/login` を使う。管理者専用のログイン画面は作らない。**

理由：ログイン経路が 2 つあると、片方の穴が全体の穴になる。既存の `/login` は
`getClaims()`（JWT 署名検証済み）を通っており、実績がある。

### 画面構成

```
src/app/(admin)/admin/            ← 新しい Route Group
  layout.tsx                      ← requireAdmin() + 管理用サイドナビ
  page.tsx                        ← ダッシュボード
  students/page.tsx               ← 受講生一覧
  students/[userId]/page.tsx      ← 受講生詳細（権限・期限・進捗）
  students/invite/page.tsx        ← 招待
  lessons/page.tsx                ← レッスン一覧（アップロード状況つき）
  lessons/[lessonId]/page.tsx     ← レッスン編集・動画アップロード
  announcements/page.tsx          ← お知らせ一覧
  announcements/[id]/page.tsx     ← お知らせ編集
```

`(main)` とは別の Route Group にする。受講生向けヘッダー・フッターを引きずらないため。

### 三重の防御

| 層 | 何をするか |
| --- | --- |
| 1. `proxy.ts` | `/admin` へのリクエストで未ログインなら `/login` へ。**早期に落とす** |
| 2. `(admin)/layout.tsx` | `requireAdmin()`。admin でなければ `/` へ redirect |
| 3. RLS | 仮に 1・2 を抜けても、DB が admin 以外の書き込みを拒否する |

3 が本丸で、1・2 は UX とコスト削減。**「画面を隠すこと」をセキュリティにしない。**

### Secret Key の扱い（重要）

管理操作は**原則すべてログイン中の admin ユーザーの RLS 経由**で行う。
Secret Key（RLS 貫通）を使うのは **受講生の招待 1 経路だけ**。

```
理由: auth.admin.inviteUserByEmail() は Admin API でしか呼べない
対策: 呼ぶ直前に必ず requireAdmin() を通す
      createAdminClient() は import "server-only" 済み
```

これにより「Secret Key が使われる場所」がコード上 1 箇所に固定され、レビューできる。

### 管理者の増やし方は SQL のままにする

`profiles.role` を画面から変更できるようにすると、
**管理画面の脆弱性 1 つが権限昇格に直結する**。

管理者を増やすのは実質「ほぼ無し」の頻度なので、SQL Editor での手動運用を維持する。
これは手抜きではなく意図的な設計判断。

---

## 4. 各画面の仕様

### 4-1. ダッシュボード

**グラフは置かない。「今なにをすべきか」だけを出す。**

```
◆ 受講生            32 名（受講中 28 / 期限切れ 4）
◆ 今週の学習         12 名がアクティブ

⚠ 要対応
  ・動画が未設定のレッスン        89 本  → レッスン一覧へ
  ・尺が未取得のレッスン           90 本  → 一括取得
  ・7 日以内に期限切れ             3 名  → 受講生一覧へ
  ・未ログインの招待               2 件  → 招待一覧へ
  ・下書きのお知らせ               1 件  → お知らせへ
```

「⚠ 要対応」が 0 件になれば運営として健全、という読み方ができる形にする。

### 4-2. 受講生一覧

| 列 | 出典 |
| --- | --- |
| 表示名 | `profiles.display_name` |
| メール | `auth.users.email`（→ admin 用 view が必要） |
| 状態 | 受講中 / 期限切れ / 未ログイン（招待済み） |
| 受講コース | 有効な enrollments の件数 |
| 最終学習日 | `lesson_progress.last_viewed_at` の最大（JST 表示） |
| 進捗 | 完了レッスン数 / 受講コースの総レッスン数 |

- 名前・メールでの絞り込み
- 状態での絞り込み（期限切れだけ表示 など）
- 行クリックで詳細へ

### 4-3. 受講生詳細 — 問い合わせ対応の主戦場

```
テスト さん  santya19930406@gmail.com
最終ログイン 2026/08/21  ・  最終学習 2026/08/21

── 受講コース ──────────────────────
 ☑ Premiere Pro 実践コース      無期限        [解除]
 ☑ CapCut 実践コース            2026/12/31    [変更] [解除]
 ☐ AI 編集コース                              [付与]

  期限プリセット: [無期限] [3ヶ月] [6ヶ月] [1年] [日付を指定]

── 学習状況 ────────────────────────
 Premiere Pro 実践コース   4 / 18 本   22%
 CapCut 実践コース         0 / 12 本    0%
```

「見られません」の問い合わせに対して、**この 1 画面で原因が分かり、その場で直せる**ことを最優先にする。

### 4-4. 受講生の招待

```
メールアドレス  [                    ]
受講コース      ☑ Premiere Pro 実践コース
                ☐ CapCut 実践コース
期限            [無期限 ▾]

               [招待メールを送る]
```

**招待とコース付与を必ず同じ画面で行う。** 別作業にすると付与漏れが必ず起きる。

処理の流れ：

1. `requireAdmin()`
2. `createAdminClient().auth.admin.inviteUserByEmail(email, { redirectTo })`
3. 返ってきた `user.id` でその場で `user_course_enrollments` を insert
4. 失敗したら招待自体をロールバックせず、**「招待は成功・権限付与は失敗」を画面に明示**する
   （招待メールは取り消せないため、嘘の成功を出さない）

既存ユーザーへの再招待は `inviteUserByEmail` がエラーになるので、
「既に登録済みです。権限を付与しますか？」に分岐させる。

### 4-5. レッスン一覧・レッスン編集

一覧は**アップロード状況が一目で分かる**ことを最優先にする。

```
コース: Premiere Pro 実践コース ▾

 #  タイトル                         動画      尺        公開
 01 このコースの進め方               ✅ ready  04:20     公開
 02 必要な機材とソフトの準備         ✅ ready  06:05     公開
 03 プロジェクトの作成と素材の読み込み ⚠ 未設定  --        下書き
 04 タイムラインの基本操作           ⏳ 処理中  --        下書き
```

編集画面でできること：

- タイトル / 説明 / 学べること / ツール / カテゴリ / レベル
- 公開・下書きの切り替え
- 並び替え（`sort_order`）
- **動画のアップロード**（→ 5 章）
- **Cloudflare から情報を取得**（尺・処理状況の再同期）

### 4-6. お知らせの作成・編集

本文は `jsonb` のブロック配列。**リッチテキストエディタは作らない。**

理由：Phase 6 で `AnnouncementBlock[]` という型が既にあり、一覧・詳細の描画も動いている。
WYSIWYG を入れると型が崩れ、既存の描画を作り直すことになる。

```
本文
 ┌──────────────────────────────┐
 │ [見出し] 8 月の新着コースについて       [↑][↓][削除] │
 │ [段落]   いつもご利用ありがとう…        [↑][↓][削除] │
 │ [箇条書き] ・AI 編集コース              [↑][↓][削除] │
 └──────────────────────────────┘
 [＋ 見出し] [＋ 段落] [＋ 箇条書き]
```

- カテゴリ、公開日時（**JST で入力 → timestamptz へ変換**）
- 「公開する / 下書きにする」の切り替え
- 公開日時を未来にすれば予約公開になる（`is_published` + `published_at` の既存設計で表現できる）

### 4-7. 学習状況（管理者が最低限見るべきもの）

**運営判断に使えるものだけに絞る。**

| 見るもの | なぜ必要か |
| --- | --- |
| 受講生ごとのコース別進捗率 | 個別フォローの判断 |
| 受講生ごとの最終学習日 | 離脱の予兆 |
| **レッスン別の視聴人数と完了率** | **どのレッスンで脱落しているかが分かる** |
| 直近 7 日のアクティブ受講生数 | 全体の健康状態 |

このうち「レッスン別の完了率」が最も価値が高い。教材改善の唯一の手がかりになる。

---

## 5. Cloudflare Stream 連携

### 5-1. アップロード方式 — Direct Creator Upload

**動画を Next.js / Vercel に通さない。**

```
1. 管理画面が Route Handler を叩く
2. サーバーが Cloudflare API を叩き、一度きりのアップロード URL を発行
     POST /accounts/{account_id}/stream/direct_upload
     { maxDurationSeconds, requireSignedURLs: true, meta: { lessonSlug } }
3. レスポンスに uploadURL と uid が入っている
4. サーバーが即座に lessons.stream_video_id = uid を書き込む
5. ブラウザが uploadURL へ直接ファイルを PUT する
```

**この方式を選ぶ理由**

| 理由 | 内容 |
| --- | --- |
| サイズ制限 | Vercel の関数にはリクエストボディ上限がある。数百 MB の動画は通せない |
| **UID が発行時に確定する** | 「アップロード後に UID を探して貼る」照合処理が**丸ごと不要になる** |
| 署名必須を維持できる | `requireSignedURLs: true` を発行時に指定でき、Phase 4 の前提を崩さない |

### 5-2. Video UID の自動紐付け（→ 照合は不要）

上記 4 の時点で紐付けが完了しているため、**「アップロード完了後の紐付け処理」は存在しない**。

ただしアップロードが中断されると「UID はあるが動画が無い」状態になる。
これを表現するために `lessons.stream_status` を追加する。

```
pending  … URL を発行した。まだアップロード中 / 中断したかもしれない
ready    … Cloudflare が readyToStream = true を返した
error    … Cloudflare がエラーを返した
```

**`ready` でないレッスンは公開できない**ようにする。これで「再生できない動画が公開される」事故を防ぐ。

### 5-3. 実尺の自動取得（`duration_seconds`）

Cloudflare の `GET /accounts/{account_id}/stream/{uid}` が返す値を使う。

```
duration        … 秒（小数）
readyToStream   … 変換完了フラグ
```

**取得のタイミング（3 段構え）**

| 方式 | 優先度 | 内容 |
| --- | --- | --- |
| アップロード後のポーリング | 🔴 必須 | 変換完了まで数十秒〜数分。画面で「処理中」を出しつつ確認する |
| 手動の「情報を取得」ボタン | 🔴 必須 | ポーリングが途切れても必ず復旧できる逃げ道 |
| Webhook | 🟡 できれば | 署名検証が必要。無くても上 2 つで足りる |

**保存**: `Math.round(duration)`。取得時刻を `stream_synced_at` に記録する。

### 5-4. 既存 90 件の不一致をどう解消するか

管理画面に「**全レッスンの動画情報を一括再取得**」を置く。
Phase 6 で先送りした `duration_seconds` の不一致は、これ 1 回で解消する。

**Phase 5 への影響**

- 90% 完了判定は **プレイヤーが報告する実尺を優先**する実装になっているため、
  DB の値を直しても判定は壊れない（むしろ正しくなる）
- 総学習時間の集計は DB の尺を使うため、**数字が変わる**。これは正しい方向の変化
- ただし **一度 `completed` になった進捗を取り消してはならない**。
  尺が伸びた結果「完了が未完了に戻る」のは受講生にとって理不尽なため、
  一括更新では `status` に触れない

### 5-5. 必要な環境変数（名前のみ・値は Vercel Dashboard で入力）

現在は署名再生用の 3 つしか無い。Phase 7 では API を叩くため 2 つ追加が必要。

| 変数名 | 用途 | 現状 |
| --- | --- | --- |
| `CLOUDFLARE_STREAM_CUSTOMER_CODE` | 再生 URL の組み立て | 設定済み |
| `CLOUDFLARE_STREAM_SIGNING_KEY_ID` | 署名 | 設定済み |
| `CLOUDFLARE_STREAM_SIGNING_KEY_PEM` | 署名 | 設定済み |
| `CLOUDFLARE_ACCOUNT_ID` | API のエンドポイント | **要追加** |
| `CLOUDFLARE_STREAM_API_TOKEN` | API 認証 | **要追加** |
| `SUPABASE_SECRET_KEY` | 招待のみ | **要追加** |

`CLOUDFLARE_STREAM_API_TOKEN` は **Stream:Edit だけの権限**で発行すること。
アカウント全体を操作できるトークンは使わない。

---

## 6. DB 変更

### 必須

| # | 変更 | 理由 |
| --- | --- | --- |
| 1 | `lessons.stream_status`（enum: pending/ready/error）追加 | アップロード中断を表現する |
| 2 | `lessons.stream_synced_at`（timestamptz, null 可）追加 | いつ Cloudflare と同期したか |
| 3 | `lesson_progress` に **admin 用 SELECT ポリシー**を追加 | 現在 `auth.uid() = user_id` のみで、admin が他人の進捗を読めない |
| 4 | `lesson_view_events` に同上 | 学習状況の集計に必要 |
| 5 | `admin_user_overview` view の追加 | `auth.users.email` / `last_sign_in_at` を admin にだけ見せる |

**3・4 は「RLS を緩める」変更ではない。** 既存の本人ポリシーはそのまま残し、
`is_admin()` を条件とする**別のポリシーを追加**する。student の見え方は一切変わらない。

**5 の view は二重にガードする**

```
security_invoker = true          … 呼び出し元の権限で評価される
where public.is_admin()          … view 定義の中でも弾く
```

`profiles` に email 列を複製する案は採らない。二重管理になり、必ず食い違うため。

### できれば

| # | 変更 | 理由 |
| --- | --- | --- |
| 6 | `admin_audit_log` | 誰がいつ誰の権限を変えたか |
| 7 | `lessons.duration_source`（cloudflare / manual） | 手で直した値を一括更新が踏み潰さないように |

---

## 7. 運用フロー（Phase 7 完了後）

### 新しい受講生が来たとき

```
管理画面 → 受講生 → 招待
  メールを入力 → 受講コースを選ぶ → 期限を選ぶ → 送信
                                    ↓
                      受講生が招待メールからパスワードを設定
                                    ↓
                              すぐに視聴できる
```

Supabase Dashboard も SQL Editor も開かない。

### 新しい動画を追加するとき

```
管理画面 → レッスン → 対象のレッスンを開く
  → ファイルを選ぶ → アップロード
      （UID は自動で紐付く / 尺は変換完了後に自動で入る）
  → ready になったら「公開」に切り替える
```

Cloudflare Dashboard も SQL Editor も開かない。

### 「動画が見られません」と言われたとき

```
管理画面 → 受講生 → 名前で検索 → 詳細
  受講コースと期限がその場で見える
  → 足りなければその場で付与・延長する
```

---

## 8. Custom SMTP をどうするか

### 結論：**Phase 7 に含めるべき。というより前提条件。**

| # | 理由 |
| --- | --- |
| 1 | Supabase の共有 SMTP は**開発用**で、送信レート制限が厳しい。本番の招待に耐えない |
| 2 | **招待メールが届かない = 受講生が入れない = サービスが始まらない** |
| 3 | 差出人が Supabase 側のドメインだと迷惑メールに入りやすい |
| 4 | メールテンプレートを日本語にできる（現在は英語の既定文面） |
| 5 | `src/lib/supabase/email-link.ts` のコメントにあるとおり、公式推奨の `token_hash` 方式へ移行でき、暗黙フローの回避策を廃止できる |

### ただし私（Claude）の判断では契約しません

外部サービスへの新規契約はご指示で禁止されているため、**候補の比較だけ**を用意する。

| 候補 | 特徴 |
| --- | --- |
| Resend | 設定が最も簡単。Supabase の公式ドキュメントでも例示されている |
| SendGrid | 実績が長い。到達率のレポートが厚い |
| Amazon SES | 最安。ただし審査（サンドボックス解除）に時間がかかる |

**判断が必要なのは「どれを契約するか」だけ**で、実装側の作業はどれを選んでも同じ。

---

## 9. 実装順

各段階で `npm test` / TypeScript / ESLint / 本番ビルド を通す。
専用ブランチ `feature/phase7-admin` で進め、全段階が通ってから main へ。

| 段階 | 内容 | 完了時に何ができるか |
| --- | --- | --- |
| **7-A** | 管理画面の土台（Route Group / `requireAdmin()` / proxy の早期拒否 / 空のダッシュボード） | admin だけが `/admin` に入れる |
| **7-B** | admin 用 RLS ポリシーと view / 受講生一覧・詳細（**読み取りのみ**） | 誰がどのコースを受講しているか画面で分かる |
| **7-C** | 受講権限の付与・解除・期限（**書き込み**） | **最頻業務が SQL を卒業する** |
| **7-D** | 受講生の招待（Secret Key 経路） | 招待から視聴開始まで画面で完結（※ SMTP 判断が要る） |
| **7-E** | Cloudflare アップロード + UID 自動紐付け + 尺の自動取得 | 動画追加が画面で完結する |
| **7-F** | 既存 90 件の尺を一括再取得 | **Phase 6 で先送りした不一致が解消する** |
| **7-G** | お知らせの作成・編集・公開 | お知らせが SQL を卒業する |
| **7-H** | レッスン編集・並び替え・公開 + ダッシュボード仕上げ | 「⚠ 要対応」が機能する |

**7-C が終わった時点で、日常業務のうち最も頻度の高いものが SQL を卒業する。**
ここを早く置くために、7-B は読み取りだけに絞って先に通す。

---

## 10. リスク

| # | リスク | 影響 | 対策 |
| --- | --- | --- | --- |
| 1 | **Secret Key を Vercel に置く** | 漏れれば全データが読み書きされる | 使用箇所を招待の 1 経路に限定。`server-only` + `requireAdmin()` の二重。他は全部 RLS 経由 |
| 2 | Cloudflare API Token の権限過大 | アカウント全体を操作されうる | **Stream:Edit のみ**のトークンを新規発行する |
| 3 | 招待メールが届かない | 受講生が入れない | Custom SMTP（→ 8 章）。届かない場合の再送導線も用意 |
| 4 | アップロード中断で UID だけ残る | 再生できない動画が公開される | `stream_status` で表現し、`ready` 以外は公開させない |
| 5 | 尺の一括更新が進捗に影響する | 「完了」が「未完了」に戻る | 一括更新では `status` に触れない。位置と尺だけ直す |
| 6 | 管理画面での誤操作（権限の一括解除など） | 受講生が締め出される | 解除時に確認ダイアログ + **対象人数を明示**する |
| 7 | `auth.users` を view で露出する | メールアドレスの漏洩 | `security_invoker` と `is_admin()` の二重ガード |
| 8 | 管理画面が受講生向けの表示を壊す | 全体障害 | `(admin)` を別 Route Group にし、`(main)` のコンポーネントを改変しない |
| 9 | admin が自分の権限を誤って落とす | 誰も管理できなくなる | **role 変更を画面に置かない**（→ 3 章）ことで構造的に防ぐ |

---

## 11. Phase 7 完了時にできる状態

### 運営スタッフが Supabase / Cloudflare を開かずにできること

- ✅ 新しい受講生を招待し、**その場で**受講コースと期限を設定する
- ✅ 「見られません」の問い合わせを 1 画面で調査し、その場で直す
- ✅ 受講期限を延長・停止する。期限切れ間近を事前に把握する
- ✅ 新しい動画をアップロードし、公開まで完結する（UID も尺も自動）
- ✅ お知らせを書き、公開日を決めて出す（予約公開も可）
- ✅ 誰がどこまで進んでいるか、**どのレッスンで脱落しているか**を見る
- ✅ 「⚠ 要対応」で、動画未設定・期限切れ間近・未ログインの招待に気づく

### SQL が残るもの（意図的に残す）

| 残るもの | 頻度 | なぜ残すか |
| --- | --- | --- |
| 新しいコースの立ち上げ | 数ヶ月 | 画面化のコストに見合わない |
| カテゴリ・ツールの追加 | 数ヶ月 | デザイン値を含み、画面化の価値が低い |
| 管理者の追加 | ほぼ無し | **権限昇格を画面から不可能にするため**（安全側の設計） |

### Phase 8 以降の候補

- 決済連携（決済完了 → 自動招待 + 自動付与）。`createAdminClient()` は既にこの入口を想定している
- 修了証の発行
- プレイヤーのデザイン刷新
- 受講生ごと・コースごとのお知らせ配信（`target_course_id` を 1 列足すだけで拡張できる）
