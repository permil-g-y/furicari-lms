# 既存教材統合 実装計画書

> 作成: 2026-08-25 / 対象: **Phase 7 完成版**（`main` = Vercel Production）
> ステータス: **計画のみ。コード変更・migration・Supabase 変更・本番変更は未実施**

## この資料の位置づけ

| 資料 | 役割 |
| --- | --- |
| `FURICARI_HANDOFF.md` | **プロジェクト全体の現況**。Phase 7 完成版の姿と回帰テスト。統合が完了するまで「統合済み」には更新しない |
| `FURICARI_LEGACY_CONTENT_INTEGRATION.md` | **実物調査と設計判断**。Notion に何があるか、なぜこの設計にするか |
| **`FURICARI_LEGACY_INTEGRATION_PLAN.md`（本書）** | **これからどう実装・移行するか**。Phase 分割・手順・受け入れ条件 |

実装を担当するセッションは、**本書と `FURICARI_HANDOFF.md` の 2 つを読めば着手できる**。
背景を知りたいときだけ `FURICARI_LEGACY_CONTENT_INTEGRATION.md` を参照する。

---

## 0. 最重要方針（実装者は必ず読むこと）

### 既存の動画レッスンに視覚的差分を発生させないこと

これが**本統合の最優先の受け入れ条件**である。機能追加より優先する。

- `lesson_type = 'video'` のとき、**現在のレッスン画面をそのまま表示する**
- 記事・課題対応を理由に、以下を再設計してはならない
  - 動画プレイヤーのサイズ・位置 / レイアウト / 余白 / タイトル位置 / タグ
  - 右カラムのカリキュラム / 前へ・次へ / お気に入り / 完了操作
  - PC・Mobile のレイアウトと現在のレスポンシブ挙動
- **全レッスンを新しい共通デザインに寄せる、という発想を取らない**

### 分岐は「上位の安全な層」で行う

`StreamStage` と `VideoPlayer` / `MobileVideoPlayer` の**内部には一切触れない**。
Phase 5・6 で最も壊れた箇所であり、ここを触ると視覚的差分の検証が難しくなる。

---

## 1. 追加調査でわかったこと（本計画の前提）

`FURICARI_LEGACY_CONTENT_INTEGRATION.md` に加えて、本計画のために実測した。

| # | 項目 | 実測結果 | 計画への影響 |
| --- | --- | --- | --- |
| 1 | **Notion のエクスポート可否** | **ゲスト権限でも「エクスポート」メニューが利用可能** | 移行方式（Export 前提）が**成立する** |
| 2 | **画像の実容量** | 平均 **129KB / 枚**、1 課題あたり **約 2.6MB** | 課題 27 本で **約 70MB**、Tips を足しても **100〜200MB** |
| 3 | **画像の解像度** | **4800×7200 の巨大画像が混在** | **リサイズ処理が必須**（そのまま配ると受講生の通信量が跳ねる） |
| 4 | **Supabase Storage の現況** | **バケット 0。一度も使っていない** | 無料枠 1GB に対し十分。**新規バケットの作成から始める** |
| 5 | **プレイヤーの継ぎ目** | PC は `WatchView.tsx` 内の `<VideoPlayer/>` **単一要素**、Mobile は `<MobileVideoPlayer/>` **単一要素** | **スロット化で 2 行の差分に収まる**（後述） |
| 6 | **Mobile の没入レイアウト** | `AppShell.resolveShell()` が `/watch/` で `mobileHeader: null, mobileTab: null` を返す | 記事ではヘッダーを出す必要があり、**ここも分岐が要る** |
| 7 | **検索の対象** | title / description / keyPoints / チャプター名 / コース名 / ツール / カテゴリ | **本文は検索対象外のまま**にできる（初期スコープ外） |

### まだ未確認（実装前に確認が必要）

| # | 未確認 | 誰が |
| --- | --- | --- |
| 1 | エクスポートの**実行結果**（ZIP の中身・画像のファイル名規則・サブページの階層） | 実装 X-E0 で実施 |
| 2 | 課題 27 本すべての画像枚数（1 本のサンプルからの推定） | 移行時に判明 |
| 3 | Tips 71 本の中身 | 第 2 段階で確認 |

---

## 2. 推奨アーキテクチャ

### 結論：**スロット注入方式**（`WatchView` は 2 行だけ変更）

`WatchView.tsx` のプレイヤー描画箇所は、PC・Mobile とも**単一の JSX 要素**であることを確認した。
ここを「省略可能なスロット」にする。

```tsx
// Mobile（現在は 140〜150 行目あたり）
{media?.mobile ?? (
  <MobileVideoPlayer ... />      // ← 現在のコードをそのまま残す
)}

// PC（現在は 269〜278 行目あたり）
{media?.desktop ?? (
  <VideoPlayer ... />            // ← 現在のコードをそのまま残す
)}
```

**`lesson_type = 'video'` のとき `media` は `undefined`**。
`??` の左辺が undefined なので、**評価結果は現在とまったく同じ JSX** になる。
つまり視覚的差分が発生し得ない。

記事・課題のときだけ `page.tsx` が `media` を渡す。

```tsx
// app/(main)/watch/[lessonId]/page.tsx
const media =
  lesson.lessonType === "video"
    ? undefined                                   // 何も渡さない = 現状維持
    : { desktop: <LessonBody blocks={body} />, mobile: <LessonBody blocks={body} compact /> };

return <WatchView lessonId={lessonId} playback={playback} media={media} />;
```

### なぜこの方式か（他案との比較）

| 案 | 既存動画への影響 | 重複 | 評価 |
| --- | --- | --- | --- |
| **A. スロット注入（推奨）** | **2 行の差分。video 経路は式として同一** | 無し（chrome を完全再利用） | **採用** |
| B. `ArticleView` を別途作る | **0 行。完全無変更** | chrome 約 300 行を複製。将来ズレる | 次点 |
| C. `LessonShell` へ抽出して再構成 | 大（WatchView の再設計） | 無し | **却下**。禁止された大規模リファクタ |

> B は「絶対に 1 行も触らない」を最優先するなら選べる。
> ただし前へ / 次へ・お気に入り・完了・カリキュラムを二重管理することになり、
> **将来どちらかを直したときにもう一方がズレる**。長期の壊れやすさは B のほうが高い。
> → **判断項目 ① として山田さんに確認**（推奨は A）。

### レイアウトの扱い

| 画面 | 動画レッスン | 記事・課題レッスン |
| --- | --- | --- |
| PC | 現状のまま（`grid-cols-[1fr_380px]`、右にカリキュラム） | **同じグリッドを使う**。左の中央領域に本文を流す |
| Mobile | 現状のまま（没入。ヘッダー・タブバー無し） | **ヘッダーを出す**（`AppShell.resolveShell()` に分岐を足す） |

Mobile の分岐は `AppShell` 側で行う。**`WatchView` の Mobile 側 JSX は触らない。**

---

## 3. データ構造（確定案）

```sql
-- X-A で適用（未適用）
create type public.lesson_type as enum ('video', 'article', 'assignment');

alter table public.lessons
  add column if not exists lesson_type public.lesson_type not null default 'video',
  add column if not exists body jsonb not null default '[]'::jsonb,
  -- 課題のメタ（実物の Notion データベースに存在した）
  add column if not exists assignment_pattern text,   -- 'A' | 'B' | 'C'
  add column if not exists assignment_level integer;  -- 1〜5（★の数）
```

**`default 'video'` により既存 90 本は 1 行も UPDATE されない。**

### 本文ブロックの型

```ts
export type LessonBlock =
  | { type: "heading";   text: string }
  | { type: "paragraph"; text: string }
  | { type: "list";      title?: string; items: string[] }
  | { type: "image";     path: string; alt: string; caption?: string }   // 最頻出
  | { type: "callout";   tone: "info" | "task" | "warn"; title: string; body: string }
  | { type: "link";      href: string; label: string; note?: string }    // Drive / 内部
  | { type: "embed";     provider: "vimeo"; id: string; title?: string };
```

- `image.path` は **Supabase Storage 内のパス**。完全 URL を持たない
- `file` ブロックは作らない（Google Drive を `link` で扱う）

---

## 4. 実装 Phase

各 Phase は**単独で動作確認でき、単独で revert できる**粒度にしてある。
すべて `feature/legacy-content` ブランチで進め、**main へのマージは最後にまとめて行う**（山田さん確認後）。

---

### X-A｜DB とデータ層（画面に変化なし）

| | |
| --- | --- |
| **目的** | 本文と種別を持てるようにする。既存を壊さない |
| **変更対象** | `supabase/migrations/`（新規 1 本）、`src/lib/supabase/database.types.ts` |
| **新規作成** | `src/lib/lesson/blocks.ts`（型・検証・整形の純粋関数）、`blocks.test.ts` |
| **流用** | `src/lib/admin/announcement-draft.ts` の `cleanBlocks()` の考え方 |
| **DB 変更** | 上記 4 列 + enum |
| **UI 変更** | **なし** |
| **既存動画への影響** | **なし**（既定値 `video`、画面はどこも読まない） |
| **テスト** | 空ブロックの除去 / 未知の type を捨てる / 壊れた jsonb で落ちない |
| **完了条件** | migration 適用後、既存 90 本の `lesson_type` が全て `video`。`npm test` / tsc / lint / build が通る。**受講生画面・管理画面に一切の変化がない** |
| **リスク** | 低 |
| **ロールバック** | `alter table ... drop column` の 2 行。既存データに触れていないので安全 |
| **次へ進む条件** | 本番相当のデータで migration が通り、回帰テスト A〜E が全て通ること |

---

### X-B｜本文の描画（受講生側）

| | |
| --- | --- |
| **目的** | 記事・課題が読める状態にする |
| **変更対象** | `WatchView.tsx`（**2 行のみ**）、`AppShell.tsx`（Mobile ヘッダー分岐）、`watch/[lessonId]/page.tsx` |
| **新規作成** | `src/components/lesson/LessonBody.tsx` とブロック別の小部品 |
| **流用** | `WatchView` の chrome 一式（タイトル・タグ・カリキュラム・前後・お気に入り・完了）、`Tag` / `Button` |
| **DB 変更** | なし |
| **UI 変更** | 記事・課題のときだけ中央領域が本文になる |
| **既存動画への影響** | **式として同一（`media ?? 既存 JSX`）。視覚的差分ゼロが受け入れ条件** |
| **テスト** | 全ブロック種別の描画 / 未知 type を無視 / 画像の遅延読み込み / Mobile でヘッダーが出る |
| **完了条件** | **X-Z の視覚回帰（後述）で動画レッスンの差分ゼロ**。テスト用の記事レッスンを 1 本手で作り、PC/Mobile で読めること |
| **リスク** | **本計画の最大の山**。プレイヤー周辺の描画順に影響しないことを実測で確認する |
| **ロールバック** | `media` prop を消して `page.tsx` を戻す（2 ファイル） |
| **次へ進む条件** | 視覚回帰で差分ゼロ。回帰テスト **C（動画）** が全て通ること |

**この Phase で絶対にやらないこと**
- `StreamStage` / `VideoPlayer` / `MobileVideoPlayer` / `useIsDesktopLayout` の変更
- 動画レッスンの余白・タグ・カリキュラムの調整
- 「ついでに」のリファクタリング

---

### X-C｜画像ホスティング

| | |
| --- | --- |
| **目的** | 500〜700 枚の画像を配れる状態にする |
| **変更対象** | なし（新規経路のみ） |
| **新規作成** | Supabase Storage バケット `lesson-assets`、`src/lib/lesson/asset-url.ts`、`scripts/upload-lesson-assets.mjs` |
| **流用** | `VideoUploader` の進捗表示の作り |
| **DB 変更** | なし |
| **UI 変更** | なし（X-B の `image` ブロックが表示できるようになる） |
| **既存動画への影響** | **なし** |
| **テスト** | パス → URL の変換 / 存在しない画像で落ちない |
| **完了条件** | サンプル 10 枚をアップロードし、記事レッスンで表示できること |
| **リスク** | 中。**巨大画像（4800×7200）のリサイズを必ず入れる**。入れないと受講生の通信量が跳ねる |
| **ロールバック** | バケットごと削除（他機能に影響しない） |

**バケット方針（判断項目 ② で確定）**
- 公開バケット + 推測困難なパス … 実装が単純。**推奨**
- 非公開バケット + 署名付き URL … 受講権限で守れるが、本文中の全画像に署名発行が要り重い

---

### X-D｜管理画面のブロックエディタ

| | |
| --- | --- |
| **目的** | 運営が SQL を書かずに教材を作れる |
| **変更対象** | `admin/lessons/[lessonId]/page.tsx`、`LessonFieldsForm.tsx` |
| **新規作成** | `LessonBodyEditor.tsx`（`AnnouncementEditor` を流用）、画像の複数枚アップロード |
| **流用** | **`AnnouncementEditor` のブロック追加・並べ替え・削除がほぼそのまま使える**。`cleanBlocks()` / 公開切替 / 並び替え |
| **DB 変更** | なし |
| **UI 変更** | 管理画面のみ |
| **既存動画への影響** | **なし**（動画カードは `lesson_type = 'video'` のときだけ出す） |
| **テスト** | 保存 → 受講生側へ反映 / 空ブロックが落ちる / 種別切替で動画カードの出し分け |
| **完了条件** | 管理画面だけで記事レッスンを 1 本作って公開できること |
| **リスク** | 中。**作り込みすぎ**。Notion を目指さない |
| **ロールバック** | エディタを外す（既存のレッスン編集は残る） |

---

### X-E｜移行（半自動）

**X-E0：エクスポートと下ごしらえ（自動）**

```
1. Notion で「授業課題一覧」を Markdown & CSV でエクスポート（画像同梱）  ← 人
2. ZIP を展開し、構造を検証するスクリプトを流す                          ← 自動
3. 画像を長辺 1600px へリサイズ（巨大画像対策）                          ← 自動
4. Supabase Storage へ一括アップロード                                   ← 自動
5. Markdown 内の画像参照を Storage のパスへ置換                          ← 自動
6. Markdown → LessonBlock[] へ変換し、JSON として出力                    ← 自動
```

- **500〜700 枚を人が 1 枚ずつ触る工程は作らない**
- 3〜6 は `scripts/` に置く一度きりのスクリプト。**アプリ本体には入れない**

**X-E1：投入と仕上げ（人の判断）**

```
7. 生成した JSON を管理画面から取り込む（または直接 insert）  ← 半自動
8. 人が 1 レッスンずつ確認して整える                          ← 人
     ・見出しの粒度
     ・不要な記述（Notion 固有の言い回し）の削除
     ・内部リンクの張り直し（Notion URL → フリキャリの /watch/...）
     ・Vimeo 埋め込みの確認
     ・Google Drive リンクの確認
9. 下書きのまま確認 → 公開                                    ← 人
```

| | |
| --- | --- |
| **想定作業量** | 課題 27 本 × 15〜30 分 = **7〜14 時間**（第 1 段階） |
| **既存動画への影響** | なし（新規レッスンの追加のみ） |
| **リスク** | 中途半端な状態での公開 → **必ず下書きで作る**（Phase 7-H で実装済み） |
| **ロールバック** | 追加したレッスンを下書きに戻す。既存には触れていない |

---

### X-Z｜動画レッスンの回帰保証（**X-B の前後で必ず実施**）

**これは独立した Phase ではなく、X-B の受け入れ検査**として扱う。

**手順**

```
1. X-B に着手する前に、現在の動画レッスンを撮影しておく（基準）
     - PC 1440 × /watch/premiere-practice-05
     - Mobile 390 × 同上
     - 実動画のあるレッスンで行う（ダミープレイヤーでは意味が薄い）

2. X-B 実装後、同じ条件で撮影する

3. 2 枚を比較する
```

**比較方法（判断項目 ③）**
- (a) スクリーンショットを目視で並べて比較 … 実装が不要。**まずはこれで足りる**
- (b) ピクセル差分を機械的に取る … 確実だが、動画のフレームやアニメーションで誤検知が出る

**加えて、DOM レベルの数値比較を必ず行う**（目視より確実で、実装も軽い）

| 測定項目 | 期待 |
| --- | --- |
| Cloudflare iframe の数 | 前後で同じ（**1 つ**） |
| iframe の実寸とアスペクト比 | 前後で同じ（16:9） |
| 中央カラムの幅 | 前後で同じ |
| タグの個数と文言 | 前後で同じ |
| カリキュラムの行数 | 前後で同じ |
| 横スクロールの有無 | 前後とも無し |

**受け入れ条件**：上記がすべて一致し、`FURICARI_HANDOFF.md` の回帰テスト **A〜E** が全て通ること。
**一致しなければ X-C 以降へ進まない。**

---

## 5. Phase の順番を再検証した結果

調査書の案（A → B → C → D → E → F）を実コードで見直した。**順番は妥当**だが、2 点を修正する。

| # | 修正 | 理由 |
| --- | --- | --- |
| 1 | **X-C（画像）を X-B（本文表示）の後ろに置く** | X-B は画像なしのブロック（見出し・段落・箇条書き）だけで完成させられる。画像を待たずに**最大の山を先に越える**ほうが安全 |
| 2 | **X-F（総合 QA）を独立 Phase にせず、X-B の受け入れ条件（X-Z）に前倒しする** | 「最後にまとめて QA」だと、動画が壊れていた場合の原因切り分けが難しい。**壊れやすい Phase の直後に検査する** |

**最終的な順番**

```
X-A（DB・画面変化なし）
  → X-B（本文表示 ＋ X-Z 視覚回帰）   ← 最大の山。ここを越えれば残りは低リスク
    → X-C（画像）
      → X-D（管理画面）
        → X-E0（移行スクリプト・自動）
          → X-E1（投入と仕上げ・人）
            → 総合 QA → main へマージ（山田さん確認後）
```

---

## 6. 移行範囲の整理

| 対象 | 件数 | 扱い | 理由 |
| --- | --- | --- | --- |
| **課題** | **27** | **第 1 段階で Lesson へ移行** | 教材の本体 |
| **Tips** | **71** | **第 2 段階で Lesson へ移行** | 補助教材。課題から参照されるため、いずれ必要 |
| FAQ | 6+ | **移行しない**。お知らせ or 静的ページ | 学習単位ではない。進捗を取る意味がない |
| 運営からのお知らせ | — | **お知らせ機能へ** | Phase 6 で実装済みの機能で足りる |
| 直近のイベント（カレンダー） | — | **移行しない** | お知らせで代替。カレンダー UI を作る価値が薄い |
| セミナー / 勉強会 | — | **保留**（第 2 段階で判断） | 未訪問 |
| 学習の進め方 | — | **記事 Lesson 1 本 or 静的ページ** | 判断項目 ④ |

### 外部サービスの扱い（基本案どおり）

| | 方針 | 問題点 |
| --- | --- | --- |
| Google Drive（素材 ZIP） | **当面リンクのまま** | **受講権限で守られない**。期限切れの受講生も開ける。現状も同じなので**悪化はしない** |
| Vimeo（完成見本） | **当面 Vimeo のまま** | 動画が 2 系統になる。Cloudflare Stream へ寄せるかは別判断 |
| Notion | **最終的にフリキャリへ一本化** | 併存すると二重管理 |

---

## 7. Vimeo 埋め込みの実装方法

`embed` ブロックで扱う。**Cloudflare Stream の仕組みには一切触れない。**

```tsx
<iframe
  src={`https://player.vimeo.com/video/${id}`}
  allow="autoplay; fullscreen; picture-in-picture"
  className="absolute inset-0 h-full w-full"
/>
```

- 16:9 のコンテナに入れる（既存の `bg-player` の見た目を流用してよい）
- **CSP は設定していない**ため追加設定は不要（`next.config.ts` を確認済み）
- 進捗には**関与させない**。Vimeo の再生位置は取らない（完了は手動）

---

## 8. 既存機能への影響と対処

| 機能 | 影響 | 対処 | Phase |
| --- | --- | --- | --- |
| 動画視聴 / Cloudflare Stream | **なし** | 触らない | — |
| 受講権限 / RLS | **なし** | コース単位のまま | — |
| 進捗（`lesson_progress`） | 小 | 記事では `position_seconds` は 0 のまま。既存ロジックがそのまま動く | X-B |
| 完了判定 | 小 | 90% 自動完了は動画のみ。記事は手動（実装済みの経路） | X-B |
| コース進捗率 | 中 | 記事も 1 本と数えれば整合。**X-B で実測確認** | X-B |
| **総学習時間** | **中** | `duration_seconds` の合計。記事が 0 だと伸びない → **目安時間を入力できるようにする** | X-D |
| **動画一覧 / 検索** | **中** | 記事が混ざる。**判断項目 ⑤** | X-B |
| サムネイルの尺表示 | 小 | 0 のとき非表示 | X-B |
| 管理画面 | 小 | 一覧に種別列。動画カードは video のときだけ | X-D |
| **Mobile 表示** | **中** | 没入レイアウトはプレイヤー前提。記事ではヘッダーを出す | X-B |
| 検索の本文対応 | — | **初期スコープ外**。タイトル・説明・学べることのみ対象のまま | — |

---

## 9. リスクとロールバック

| # | リスク | 影響 | 対策 | ロールバック |
| --- | --- | --- | --- | --- |
| 1 | **X-B で動画レッスンが壊れる** | **大** | スロット方式（式として同一）+ X-Z の視覚回帰 | `media` prop を消す（2 ファイル） |
| 2 | 画像の移行が終わらない | 大 | Export + スクリプト前提。手作業にしない | 移行を止めても既存機能は無傷 |
| 3 | Notion の画像 URL が期限切れ | 大 | 署名付き URL を参照しない。**必ず自前へコピー** | — |
| 4 | 巨大画像で通信量が跳ねる | 中 | **X-C でリサイズ必須**（長辺 1600px） | 再アップロード |
| 5 | Storage の容量 | 低 | 実測 100〜200MB 見込み。無料枠 1GB | — |
| 6 | 素材が受講権限で守られない | 中 | 現状と同じ。**悪化はしない**。守るなら判断項目 ② | — |
| 7 | 総学習時間が伸びない | 中 | 目安時間を入力可能にする | — |
| 8 | ブロック種別を増やしすぎ | 小 | 7 種類で止める | — |
| 9 | 中途半端な教材が公開される | 小 | 下書きで作る（実装済み） | 下書きへ戻す |

---

## 10. 工数

| Phase | 規模 | 目安 |
| --- | --- | --- |
| X-A DB | 小 | 0.5 日 |
| X-B 本文表示 + X-Z 視覚回帰 | 中 | **2〜2.5 日** |
| X-C 画像 | 中 | 1.5〜2 日 |
| X-D 管理画面 | 中 | 2〜3 日 |
| X-E0 移行スクリプト | 小〜中 | 1〜1.5 日 |
| 総合 QA | 小 | 0.5 日 |
| **開発計** | **中〜大** | **7.5〜10 日** |
| X-E1 移行作業（課題 27 本） | — | **7〜14 時間**（運用作業） |
| X-E1 移行作業（Tips 71 本） | — | 18〜36 時間（第 2 段階） |

---

## 11. 【実装前に山田確認が必要】

帰宅後、以下 5 点だけ判断いただければ着手できる。

---

### ① `WatchView` を 2 行だけ変更してよいか

**A 案：スロット注入（`WatchView` を 2 行変更）**
- メリット：chrome（前後移動・お気に入り・完了・カリキュラム）を完全に再利用できる。将来どちらかを直したときにズレない。`video` 経路は式として現在と同一なので視覚的差分が発生し得ない
- デメリット：`WatchView.tsx` に 2 行の差分が入る

**B 案：`ArticleView` を別コンポーネントとして新規作成（`WatchView` は 0 行）**
- メリット：既存ファイルを 1 行も触らない。心理的な安心感が最も高い
- デメリット：chrome 約 300 行を複製する。**将来どちらか一方だけ直して不整合が起きる**。今後の変更コストが恒久的に増える

**推奨：A**
**理由**：`media ?? <既存 JSX>` は、`media` が undefined のとき**評価結果が現在とまったく同じ**になる。差分は 2 行だが、動画レッスンの描画は数学的に変わらない。一方 B の重複は、時間が経つほど確実に負債になる。加えて X-Z の視覚回帰で「差分ゼロ」を実測で担保する。

---

### ② 画像を置くバケットを公開にするか

**A 案：公開バケット + 推測困難なパス**
- メリット：実装が単純。本文中の画像がそのまま表示できる。表示が速い
- デメリット：URL を知っていれば誰でも見られる（教材の**スクリーンショット**が対象）

**B 案：非公開バケット + 署名付き URL**
- メリット：受講権限で守れる
- デメリット：1 ページ 26 枚すべてに署名発行が要り、実装と表示が重くなる。URL に有効期限がありキャッシュも効きにくい

**推奨：A**
**理由**：守る価値が高いのは**動画本体**（Cloudflare で署名済み）と**素材 ZIP**であり、操作手順のスクリーンショット単体が流出しても損害は限定的。B のコストに見合わない。なお素材 ZIP は Google Drive のままなので、この判断では状況が変わらない。

---

### ③ 視覚回帰をどう確認するか

**A 案：スクリーンショットの目視比較 + DOM の数値比較**
- メリット：追加実装なし。すぐできる
- デメリット：わずかな差分（1〜2px）を見逃す可能性

**B 案：ピクセル差分の自動比較**
- メリット：見逃しがない
- デメリット：比較の仕組みを作る必要がある。動画のフレームやアニメーションで**誤検知が多発**する

**推奨：A**
**理由**：懸念しているのは「レイアウトが変わる」ことであり、1px のズレではない。DOM の数値比較（iframe 数・実寸・アスペクト比・カラム幅・タグ数・カリキュラム行数）を併用すれば、レイアウト崩れは確実に検出できる。B は動画という題材と相性が悪い。

---

### ④ 「学習の進め方」をどこに置くか

**A 案：記事 Lesson として、専用コースを 1 つ作って入れる**
- メリット：既存の仕組みだけで済む。進捗も取れる
- デメリット：「コース」が 1 つ増える。学習コースと運営案内が並ぶ

**B 案：LMS に入れず、お知らせ or 静的ページにする**
- メリット：学習コンテンツと運営案内が混ざらない
- デメリット：静的ページを作る実装が別途必要

**推奨：A**
**理由**：実物を見ると「学習の進め方」は**受講開始時に必ず読ませたい内容**で、読んだかどうかを把握したい性質のもの。進捗が取れる Lesson にする価値がある。コース名を「はじめに」等にすれば並びの違和感も小さい。

---

### ⑤ 「動画一覧」をどうするか

**A 案：「教材一覧」に改称し、種別フィルタ（すべて / 動画 / 記事・課題）を足す**
- メリット：記事・課題も発見できる。教材が増えるほど価値が上がる
- デメリット：ナビゲーションの文言が変わる（受講生から見て変化がある）

**B 案：「動画一覧」のまま。記事・課題は出さない**
- メリット：既存画面に一切変化がない
- デメリット：記事・課題への入口がコース詳細だけになり、**移行した教材が見つけにくい**

**推奨：A**
**理由**：課題 27 本と Tips 71 本を移行したあと、それらが一覧に出ないのは実用上つらい。ただしこれは**受講生から見える変化**なので、山田さんの判断が要る。B を選ぶ場合は X-B の作業がわずかに減る。

---

## 12. 実装セッションへの申し送り

- 着手前に `FURICARI_HANDOFF.md` の**回帰テスト A〜E** に目を通すこと
- **X-B に着手する前に、動画レッスンの基準スクリーンショットと DOM 数値を取っておくこと**（後から取り直せない）
- `StreamStage` / `VideoPlayer` / `MobileVideoPlayer` / `useIsDesktopLayout` は**触らない**
- 各 Phase の終わりで `npm test` / `npx tsc --noEmit` / `npx eslint src` / `npm run build` を通すこと
- **main へのマージと本番反映は、山田さんの確認を得てから行うこと**
